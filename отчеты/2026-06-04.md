# Отчет 2026-06-04

## Live исправления Google/VDS

- Создан backup VDS Postgres перед изменениями:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260604T054930Z.sql.gz`.
- Сохранены локальные backup-файлы затронутых Google-строк:
  - `outputs/diagnostics/2026-06-04-live/google_data_rows_before_0406_fix.json`;
  - `outputs/diagnostics/2026-06-04-live-after-fix/google_archive_rows_before_extra_delete_0406_terminal.json`.
- Исправлена дата нового перечисления:
  - Excel имел `ДАТА ДОСТАВКИ = 05.06.2026`;
  - VDS: `16` активных заказов из файла `Шаблон_отправки_заказов_на_склад_04_06_2026.xlsx` переведены с `2026-06-04` на `2026-06-05`;
  - Google `data`: `40` строк перечисления переведены на `05.06.2026`.
- Наведён порядок по терминалу:
  - Google `data`: удалены `87` активных терминальных дублей за `04.06.2026`;
  - Google `Архив`: удалены `5` лишних терминальных дублей за `04.06.2026`.

## Итоговая сверка

- VDS `04.06.2026 Терминал completed`: `114` позиций, `331` блок, `331` отсканирован.
- Google `Архив 04.06.2026 Терминал Выполнено`: `114` строк, `331` блок, `331` КИЗ.
- VDS `05.06.2026 Перечисление active`: `16` заказов, `40` позиций, `97` блоков.
- Google `data 05.06.2026 Перечисление Не выполнено`: `40` строк, `97` блоков.
- Pending Google queue: `0`.

## Исправление причины

- Backend importer теперь читает `Дата доставки` / `Дата отгрузки` / `Дата поставки` из строк над основной шапкой Excel.
- Desktop importer получил такую же защиту.
- Telegram import meta теперь показывает реальную дату строк, если все строки имеют одну дату из Excel.
- На VDS обновлён `backend/app/excel_importer.py`; пересобраны и перезапущены `backend-api` и `telegram-worker`.
- VDS smoke подтвердил: файл с именем `04_06` и верхней `ДАТА ДОСТАВКИ=2026-06-05` импортируется как `05.06.2026`.

## Mac ready build

- Собран `outputs/mac_ready/TakSklad-2.0.6-mac-ready`.
- Исправлен macOS frozen `APP_DIR`: `.app` теперь читает JSON рядом с `TakSklad.app`, а не пишет runtime-файлы внутрь bundle.
- Проверены `START_TAKSKLAD.command`, `START_BACKEND.command`, `START_LOCAL.command`, прямой запуск `.app`, подпись bundle и GUI-start.

## Проверки

- `./.venv/bin/python -m unittest tests.test_backend_telegram_import tests.test_excel_normalizer` - 38 tests OK.
- `./.venv/bin/python -m unittest discover -s tests` - 322 tests OK.
- `https://api.taksklad.uz/health` - OK.

## SkladBot sync

- Логика склада сохранена: заказ можно сканировать и закрывать без номера WH-R.
- Ускорено подтягивание WH-R:
  - `SKLADBOT_DETAIL_LIMIT` на VDS увеличен до `10`;
  - `SKLADBOT_COMPLETED_BACKFILL_DAYS=2`, чтобы worker догонял свежие завершенные заказы без WH-R;
  - advisory lock переведен на transaction-level lock, чтобы worker не зависал.
- После нахождения WH-R у завершенного заказа worker обновляет VDS и ставит экспорт в Google `Архив`.
- По сегодняшнему импорту `2e7702bf-eb5a-4b65-a28e-d0c4c93cb6f2` все `16/16` заказов получили WH-R и SkladBot ID.
- Google queue по `google_sheets_skladbot_export` завершена, failed events нет.
- Проверки:
  - `./.venv/bin/python -m unittest tests.test_backend_skladbot_worker tests.test_backend_google_sheets_pending tests.test_backend_api_persistence tests.test_vds_acceptance_scripts` - 108 tests OK;
  - `./.venv/bin/python -m unittest discover -s tests` - 329 tests OK;
  - `https://api.taksklad.uz/health` - OK.

## SkladBot request dry-run

- Реализован безопасный первый этап автосоздания заявок SkladBot после Telegram Excel import.
- После успешного импорта backend строит preview будущих заявок и сохраняет их в `pending_events` как `skladbot_request_dry_run`.
- Если импорт добавил только часть строк уже существующего заказа, payload собирается по всем позициям заказа.
- Реальный `POST /v1/requests` не вызывается, в event payload стоит `would_post=false`.
- Веб-кабинет получил вкладку `SkladBot dry-run`:
  - импорт;
  - клиент;
  - дата и тип оплаты;
  - адрес;
  - товары и блоки;
  - статус `ready`, `blocked`, `already_linked`;
  - причина блокировки;
  - JSON preview payload.
- В истории импортов добавлена сводка dry-run.
- Режим задается через `SKLADBOT_CREATE_REQUESTS_MODE`, default `dry_run`.
- Даже если поставить `enabled`, текущий этап оставляет фактический режим `dry_run`; боевой POST включается отдельной задачей.
- Проверки:
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_backend_skladbot_request_dry_run` - 9 tests OK;
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_backend_api_persistence` - 50 tests OK;
  - `npm run build` в `frontend` - OK;
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest discover -s tests` - 338 tests OK.

# Отчет 2026-06-05

## Forced update 2.0.8

- `version.json` переведен в принудительное обновление:
  - `latest_version=2.0.8`;
  - `min_supported_version=2.0.8`;
  - `mandatory=true`.
- `main/version.json` обновлен и запушен, потому старые приложения читают manifest именно из `main`.
- VDS-копия `version.json` синхронизирована в `/opt/taksklad/app`.
- Release/preflight/acceptance проверки переведены с staged rollout на forced rollout `2.0.8`.

## Проверки

- Unit tests по релизным guard-скриптам: `25 tests OK`.
- Release preflight с проверкой скачивания GitHub assets: `status=ok`.
- SHA256:
  - `TakSklad.exe` совпал с `version.json`;
  - `TakSklad-windows-x64.zip` совпал с `version.json`.
- Public raw manifest проверен: `mandatory=true`, `min_supported_version=2.0.8`.
- VDS health проверен: backend `2.0.8`.

## Важно

- Для уже установленных `2.0.5/2.0.6` это максимально возможное принуждение через manifest: приложение покажет, что версия больше не поддерживается и нужно обновиться.
- В старом коде `2.0.5/2.0.6` технически остается кнопка отказа. Чтобы убрать отказ полностью, нужен отдельный desktop release с hard-block логикой.

# Отчет 2026-06-05

## Live fix: очистка ошибочных КИЗов TASHKENT POINT

- Запрос: удалить ошибочно отсканированные КИЗы у клиента `"TASHKENT POINT" MCHJ`, тип оплаты `Перечисление`, дата отгрузки `08.06.2026`.
- Найден один активный VDS-заказ:
  - order_id: `4e8c73d4-5219-4832-a75f-2e65e7850458`;
  - позиции: Brown `6`, Gold `5`, RED `6` блоков;
  - записанные КИЗы были только в позиции `Chapman Gold SSL 100\`20`.
- Перед правкой создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260605T085949Z.sql.gz`.
- Локальный снимок до удаления:
  - `outputs/diagnostics/2026-06-05-tashkent-point-kiz-clear/before.tsv`.
- Через штатный backend `undo_scan` удалены `4` КИЗа с позиции Gold.
- Итоговая сверка:
  - VDS: по всем 3 позициям TASHKENT POINT `scanned_blocks=0`, `scan_codes=0`, статус `not_completed`;
  - Google `data`: строки 18-20 по TASHKENT POINT активны, `Отсканированные коды` пустые, статус `Не выполнено`;
  - Google pending events по затронутой позиции завершены.

## Forced update 2.0.8

- `version.json` переведен в принудительное обновление:
  - `latest_version=2.0.8`;
  - `min_supported_version=2.0.8`;
  - `mandatory=true`.
- `main/version.json` обновлен и запушен, потому старые приложения читают manifest именно из `main`.
- VDS-копия `version.json` синхронизирована в `/opt/taksklad/app`.
- Release/preflight/acceptance проверки переведены с staged rollout на forced rollout `2.0.8`.

## Проверки

- Unit tests по релизным guard-скриптам: `25 tests OK`.
- Release preflight с проверкой скачивания GitHub assets: `status=ok`.
- SHA256:
  - `TakSklad.exe` совпал с `version.json`;
  - `TakSklad-windows-x64.zip` совпал с `version.json`.
- Public raw manifest проверен: `mandatory=true`, `min_supported_version=2.0.8`.
- VDS health проверен: backend `2.0.8`.

## Важно

- Для уже установленных `2.0.5/2.0.6` это максимально возможное принуждение через manifest: приложение покажет, что версия больше не поддерживается и нужно обновиться.
- В старом коде `2.0.5/2.0.6` технически остается кнопка отказа. Чтобы убрать отказ полностью, нужен отдельный desktop release с hard-block логикой.

## Возвраты и SkladBot

- Реализован новый контур возврата:
  - после скана/ввода ШК накладной desktop показывает карточку заказа и SKU/блоки;
  - оператор подтверждает полный состав возврата;
  - backend проверяет, что подтвержденные SKU и блоки совпадают с исходным заказом;
  - создается отдельный pending event на SkladBot `Возврат 3PL`;
  - worker создает реальную заявку SkladBot с `request_type_id=3403`;
  - исходный WH-R отгрузки не затирается, возвратный WH-R хранится в отдельных полях.
- Google `Архив` и `Возвраты` расширены отдельными колонками для возвратной заявки SkladBot.
- Полная проверка: `./.venv/bin/python -m unittest discover -s tests` - `353 tests OK`.

## Деплой возвратов на VDS

- Перед деплоем создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260605T074030Z.sql.gz`.
- На VDS обновлен backend и перезапущены сервисы:
  - `backend-api`;
  - `skladbot-worker`;
  - `telegram-worker`;
  - `google-sheets-sync-worker`.
- Добавлена защита от дубля `Возврат 3PL` при retry:
  - если первый POST в SkladBot уже создал заявку, но ответ/детализация сорвались, повторная обработка ищет существующую заявку и не делает второй POST.
- Smoke test:
  - тестовый заказ: `TAKSKLAD_RETURN_TEST_20260605_10610b80`;
  - order_id: `916144c7-ac11-4a3e-8f79-a8dd4199ff0c`;
  - исходный тестовый WH-R: `WH-R-TEST-10610b80`;
  - созданная возвратная заявка SkladBot: `WH-R-193808`, id `193808`;
  - Google exports по архиву и возвратам завершились успешно.
- Проверки:
  - `./.venv/bin/python -m unittest tests.test_backend_skladbot_request_dry_run` - `21 tests OK`;
  - `./.venv/bin/python -m unittest discover -s tests` - `354 tests OK`;
  - VDS health: `status=ok`, backend `2.0.8`.

## Windows release 2.0.9

- Причина: для проверки возвратов через складское приложение нужна новая desktop-сборка с окном подтверждения состава возврата.
- Подготовлено:
  - `APP_VERSION=2.0.9`;
  - backend health version переведен на `2.0.9`;
  - `version.json`: `latest_version=2.0.9`, `min_supported_version=2.0.9`, `mandatory=true`;
  - release/preflight/acceptance guard'ы переведены на `2.0.9`.
- GitHub Actions:
  - release/tag: `v2.0.9`;
  - workflow run: `27003534719`;
  - Windows build завершился успешно;
  - onefile и onedir smoke-import прошли.
- SHA:
  - `TakSklad.exe`: `10ea2376ec194dc87f6007fec8a476e9444fdb04aeb79352f399aa7aca70e8f4`;
  - `TakSklad-windows-x64.zip`: `e07f4ff712ebd962922cc25e43bba499886a0b9db0fb6b74ac2a84293d5f04c3`.

## Telegram KIZ export modes

- Возвращена логика выгрузки КИЗов по загруженным Excel-файлам:
  - бот показывает все source-файлы;
  - по каждому файлу пишет прогресс `сколько/сколько` блоков отпикано;
  - выгрузочная inline-кнопка даётся только готовым файлам.
- Сохранена и расширена выгрузка по датам отгрузки:
  - кнопка `Выгрузка КИЗов` теперь сначала спрашивает режим;
  - режим `По датам отгрузки` показывает даты с уже отсканированными КИЗами и прогресс по дате;
  - прямые команды `/kiz 08.06.2026` и `/kiz 04.06.2026 05.06.2026` остаются рабочими.
- Backend больше не требует полного закрытия всей даты для выгрузки уже отсканированных КИЗов по дате.
- Перед VDS-деплоем создан restore point:
  - `/opt/taksklad/restore_points/pre-telegram-kiz-export-modes-20260605T103409Z`.
- На VDS обновлены и перезапущены:
  - `backend-api`;
  - `telegram-worker`.
- Проверка VDS после деплоя:
  - `https://api.taksklad.uz/health` вернул `status=ok`, backend `2.0.9`;
  - дата `08.06.2026` доступна для КИЗ-выгрузки как частичная: `75/160` блоков;
  - отчёт `TakSklad_КИЗ_08.06.2026.xlsx` успешно генерируется;
  - source-файлы показываются с прогрессом, готовых к выгрузке файлов: `7` из `14`.
- Проверки локально:
  - `./.venv/bin/python -m unittest discover -s tests` - `356 tests OK`;
  - `git diff --check` - OK.

## Live cleanup API test orders

- Причина: в боевом Google Sheets и Postgres оставались тестовые API/return заявки за `05.06.2026`, которые не должны попадать в рабочие сверки и отчеты.
- Перед очисткой создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260605T113027Z.sql.gz`.
- Сохранен cleanup manifest с удаляемыми строками:
  - `/opt/taksklad/backups/cleanup/20260605T113027Z/cleanup_manifest.json`.
- Из Postgres удалены тестовые данные:
  - `orders`: 2;
  - `order_items`: 3;
  - `pending_events`: 4;
  - `audit_log`: 4;
  - `imports`: 1;
  - `import_files`: 1.
- Из Google Sheets удалены 5 тестовых строк:
  - `data`: 2;
  - `Архив`: 1;
  - `Возвраты`: 1;
  - `Отмененные`: 1.
- Проверка после очистки:
  - тестовые маркеры `API TEST`, `TAKSKLAD TEST`, `taksklad-api-test`, `TAKSKLAD_RETURN_TEST`, `deploy smoke test`, `wh-r-test`, `тестовый возврат`, `тестовая` в Postgres и Google не найдены;
  - `verify_google_backend_sync.sh` больше не показывает `sheet_missing_backend` по API TEST строкам (`sheet_missing_backend_count=0`);
  - основной рассинхрон по заказам `08.06.2026` остается отдельной задачей восстановления КИЗов из Google в Postgres.

## Live KIZ recovery for 08.06.2026

- Причина: по заказам `08.06.2026` Google Sheets уже содержал пропиканные КИЗы, а Postgres держал активными позиции без сканов. Из-за этого дата не могла корректно закрываться и выгружаться.
- Подключены субагенты для параллельной проверки:
  - Google projection: восстановление только через whitelist и контроль `data`/`Архив`;
  - Postgres transaction: не использовать общий Google sync и не создавать дубли сканов;
  - safety: freeze writers, backup, manifest, rollback path.
- Перед восстановлением создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260605T113853Z.sql.gz`.
- Сохранены recovery manifests:
  - `/opt/taksklad/backups/recovery/20260605T113853Z/dry_run_manifest.json`;
  - `/opt/taksklad/backups/recovery/20260605T113853Z/apply_result.json`;
  - `/opt/taksklad/backups/recovery/20260605T113853Z/google_projection_repair_manifest.json`;
  - `/opt/taksklad/backups/recovery/20260605T113853Z/google_projection_repair_apply_result.json`.
- В Postgres восстановлены только неконфликтные КИЗы:
  - добавлено `79` scan code rows;
  - обновлено `45` позиций;
  - закрыто `22` заказа;
  - итог по дате: `29` завершенных заказов из `31`.
- Конфликтные позиции оставлены доступными для перепикивания складом:
  - `"DELTA VERTEX" MCHJ` / `Chapman RED OP 20` / `1` блок / `0` КИЗов;
  - `"TASHKENT POINT" MCHJ` / `Chapman Gold SSL 100\`20` / `5` блоков / `0` КИЗов.
- По двум активным конфликтным заказам в Google `data` восстановлена корректная проекция из VDS:
  - всего `5` активных строк;
  - `3` строки с готовыми КИЗами оставлены в статусе `Выполнено`;
  - `2` конфликтные строки очищены от КИЗов и оставлены в статусе `Не выполнено`.
- Проверки после восстановления:
  - `verify_google_backend_sync.sh --detail-limit 50` вернул `status=ok`;
  - `google_rows=5`, `backend_active_items=5`, `matched_items=5`;
  - `backend_missing_sheet_count=0`, `sheet_missing_backend_count=0`, `field_mismatch_count=0`;
  - по Postgres: `planned_blocks_0806=160`, `scanned_blocks_0806=154`, `scan_code_rows_0806=154`;
  - дублей КИЗов в Postgres нет (`duplicate_scan_codes_global=0`);
  - pending Google export после one-shot worker отсутствует.
- Итог: неконфликтные заказы `08.06.2026` закрыты, конфликтные КИЗы снова доступны для сканирования складом.

## Partial KIZ recovery for 08.06.2026 conflicts

- Причина: после перепикивания Google показывал две конфликтные позиции в `Архиве` как `Выполнено`, а VDS/Postgres оставлял их активными. Telegram считал прогресс по VDS и показывал недостачу `6` блоков.
- Перед правкой создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260605T125009Z.sql.gz`.
- Сохранен dry-run manifest:
  - `/opt/taksklad/backups/recovery/20260605T125032Z-partial-kiz-0806/dry_run_manifest.json`.
- В VDS восстановлены только `4` неконфликтных КИЗа TASHKENT POINT / 'Chapman Gold SSL 100`20'.
- Не восстановлены `2` конфликтных КИЗа:
  - DELTA VERTEX / `Chapman RED OP 20` / `1` блок;
  - TASHKENT POINT / 'Chapman Gold SSL 100`20' / `1` блок.
- Причина, почему конфликтные КИЗы не восстановлены: они уже закреплены за completed-заказом THE BIG RICH RAKAT за `04.06.2026`, поэтому повторно использовать их в VDS нельзя.
- Выполнен Google projection repair:
  - stale-строки удалены из `Архива`;
  - активные строки восстановлены в `data` по текущему VDS-состоянию;
  - failed Google export event закрыт после восстановления проекции.
- Apply/result manifests:
  - `/opt/taksklad/backups/recovery/20260605T125032Z-partial-kiz-0806/apply_result.json`;
  - `/opt/taksklad/backups/recovery/20260605T125032Z-partial-kiz-0806/google_projection_repair_before.json`;
  - `/opt/taksklad/backups/recovery/20260605T125032Z-partial-kiz-0806/google_projection_repair_apply_result.json`.
- Проверки после правки:
  - `verify_google_backend_sync.sh --detail-limit 50` вернул `status=ok`;
  - `google_rows=5`, `backend_active_items=5`, `matched_items=5`;
  - `backend_missing_sheet_count=0`, `sheet_missing_backend_count=0`, `field_mismatch_count=0`;
  - open pending Google export events: `0`;
  - отчет по дате `08.06.2026` генерируется как частичный XLSX.
- Итог: `08.06.2026` теперь `158/160`, остаток `2` блока: DELTA VERTEX RED `1` и TASHKENT POINT Gold `1`.

## Live Google to Backend sync fix for 08.06.2026 rescan

- Ситуация: склад сообщил, что перепикал оставшиеся позиции, но изменения снова появились только в Google, а VDS/Postgres остался на `158/160`.
- Причина 1: на VDS `google-sheets-sync-worker` был запущен, но `TAKSKLAD_GOOGLE_TO_BACKEND_SYNC_ENABLED` был пустой. Поэтому worker каждую минуту обрабатывал только pending Google exports и писал `rows=0 matched=0`, не читая Google `data + Архив` обратно в backend.
- Причина 2: в Google снова появились stale-строки в `Архиве` по двум конфликтным позициям. В них были те же конфликтные КИЗы, которые уже закреплены за completed-заказом THE BIG RICH RAKAT за `04.06.2026`, поэтому импортировать эти строки в Postgres нельзя.
- Перед правкой создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260605T130344Z.sql.gz`.
- Сохранен снимок Google-строк до правки:
  - `/opt/taksklad/backups/recovery/20260605T130344Z-google-backend-sync-0806/google_rows_before_repair.json`.
- Google projection repair:
  - удалены `2` stale-строки из `Архива`;
  - добавлены `2` активные строки в `data` по текущему VDS-состоянию;
  - DELTA VERTEX RED оставлен `0/1`, статус `Не выполнено`;
  - TASHKENT POINT Gold оставлен `4/5`, статус `Не выполнено`.
- На VDS включен Google -> Backend sync:
  - `TAKSKLAD_GOOGLE_TO_BACKEND_SYNC_ENABLED=true`;
  - `GOOGLE_SHEETS_SYNC_INTERVAL_SECONDS=60`;
  - `google-sheets-sync-worker` пересоздан.
- Первый проход после включения:
  - `rows=717`, `matched=717`, `missing=0`, `conflicts=0`;
  - `verify_google_backend_sync.sh --detail-limit 80` вернул `status=ok`;
  - `google_rows=5`, `backend_active_items=5`, `matched_items=5`;
  - open pending events: `0`.
- Итог: база не подтянула "новые" сканы, потому что новых валидных КИЗов в Google не было. Складу нужно нажать `Обновить` и перепикать ровно `2` оставшихся блока: DELTA VERTEX RED `1` и TASHKENT POINT Gold `1`. После этого sync должен подтянуть их в VDS автоматически в течение минуты.

## Forced conflict KIZ load for 08.06.2026

- Запрос: принудительно загрузить `2` конфликтных КИЗа, которые склад повторно отпикал в Google.
- Важное техническое ограничение: вставить эти КИЗы вторым экземпляром нельзя, потому что в Postgres `scan_codes.code` уникален. Поэтому выполнено не дублирование, а переназначение существующих `scan_codes` с заказа THE BIG RICH RAKAT за `04.06.2026` на целевые заказы `08.06.2026`.
- Перед правкой создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260605T131507Z.sql.gz`.
- Сохранены manifests:
  - `/opt/taksklad/backups/recovery/20260605T131507Z-force-conflict-kiz-0806/force_kiz_manifest_before.json`;
  - `/opt/taksklad/backups/recovery/20260605T131507Z-force-conflict-kiz-0806/force_kiz_apply_result.json`;
  - `/opt/taksklad/backups/recovery/20260605T131507Z-force-conflict-kiz-0806/google_projection_before_force_repair.json`;
  - `/opt/taksklad/backups/recovery/20260605T131507Z-force-conflict-kiz-0806/google_projection_after_force_repair_result.json`.
- Принудительно переназначены `2` КИЗа:
  - THE BIG RICH RAKAT / `Chapman RED OP 20` / `04.06.2026` -> DELTA VERTEX / `Chapman RED OP 20` / `08.06.2026`;
  - THE BIG RICH RAKAT / 'Chapman Gold SSL 100`20' / `04.06.2026` -> TASHKENT POINT / 'Chapman Gold SSL 100`20' / `08.06.2026`.
- После пересчета:
  - DELTA VERTEX за `08.06.2026` стал `completed`;
  - TASHKENT POINT за `08.06.2026` стал `completed`;
  - THE BIG RICH RAKAT за `04.06.2026` стал `not_completed`, потому что с него сняты `2` КИЗа.
- Google projection repair после переноса:
  - THE BIG RICH RAKAT удален из `Архива` и возвращен в `data` с текущим состоянием `1/3`;
  - завершенные DELTA VERTEX и TASHKENT POINT остаются в `Архиве`.
- Проверки после правки:
  - дата `08.06.2026`: `160/160`, `remaining_blocks=0`, `completed=true`;
  - source-file `заказы терминал на 08.06.2026 часть 1.xlsx`: `58/58`, `completed=true`;
  - source-file `Шаблон_отправки_заказов_на_склад_05_06_2026.xlsx`: `102/102`, `completed=true`;
  - отчет `TakSklad_КИЗ_08.06.2026.xlsx` отдается `HTTP 200`;
  - `verify_google_backend_sync.sh --detail-limit 100` вернул `status=ok`;
  - дублей КИЗов в Postgres нет;
  - open pending events: `0`.
- Последствие: дата `04.06.2026` теперь `614/616`, остаток `2` блока по THE BIG RICH RAKAT. Это прямой результат принудительного переноса этих КИЗов на `08.06.2026`.

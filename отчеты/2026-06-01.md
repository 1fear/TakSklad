# Отчёт TakSklad за 2026-06-01

## SkladBot Google Sheets re-export

- Исправлен риск рассинхрона: backend уже знает номер заявки SkladBot, но Google Sheets ещё не обновился.
- Теперь SkladBot worker, даже если все активные backend-заказы уже имеют номера, всё равно переэкспортирует эти номера в Google Sheets.
- Это важно, потому что для менеджера и склада главным видимым источником остаётся лист `data`.

## Статусы SkladBot в Google Sheets

- `found` теперь пишется как `Найдено`.
- `not_found` теперь пишется как `Не найдено`.
- `multiple` теперь пишется как `Несколько совпадений`.
- `error` теперь пишется как `Ошибка синхронизации`.

## Диагностика SkladBot

- Read-only диагностика SkladBot теперь использует активные заказы при подборе окна дат.
- Раньше диагностика могла смотреть только базовое окно, хотя реальный worker расширял окно по датам активных заказов.
- Теперь диагностика ближе к реальному поведению worker-а и лучше показывает, почему номер заявки не подтянулся.

## Проверки

- `./.venv/bin/python -m unittest tests.test_backend_skladbot_worker tests.test_backend_google_sheets_exporter tests.test_google_sheets_desktop_read tests.test_google_sheets_sync_worker` - OK, 35 тестов.
- VDS read-only проверка Google Sheets: активных строк `21`, строк с номером SkladBot `21`.
- VDS `diagnose_skladbot_match.sh` отработал и показал ближайшие несовпадения по полям `date`, `client`, `payment`, `products`.

## Что остаётся проверить вручную

- Реальный Telegram import нового Excel-файла.
- Живое SkladBot-сопоставление на новой заявке `3PL отгрузка`.
- Windows desktop acceptance на рабочем ПК.

## Version.json и уведомления об обновлении

- Старое ограничение "без `version.json` и без push-уведомлений" снято.
- Публичный `version.json` уже ведёт на `2.0.0`.
- GitHub assets из `version.json` проверены скачиванием: SHA `TakSklad.exe` и `TakSklad-windows-x64.zip` совпадает.
- Acceptance manifest фиксирует `push_notifications_allowed=true`.
- `mandatory=false` оставлен только как защита от принудительной блокировки рабочих ПК. Обычное уведомление/предложение обновления через `version.json` разрешено.

## Дополнительные проверки

- `./.venv/bin/python tools/release_preflight.py --skip-network` - OK.
- `./.venv/bin/python tools/release_preflight.py --verify-downloads --timeout 120` - OK.
- `https://api.taksklad.uz/health` - OK, backend `2.0.0`.
- VDS `./deploy/vds/acceptance_status.sh` - OK, Telegram menu live-проверка OK.

## Google Sheets ↔ backend sync gate

- Добавлен read-only verifier, который сравнивает Google Sheets `data` и активные позиции backend.
- Проверяются source keys, дата, оплата, клиент, адрес, ТП, товар, количество, SkladBot номер/ID/статус и сумма.
- Verifier добавлен в VDS acceptance, поэтому рассинхрон теперь ломает acceptance сразу.
- В verifier добавлен retry на Google Sheets `429 Quota exceeded`.

## Найденный и исправленный рассинхрон

- VDS verifier нашёл активную backend-позицию, которой уже не было в Google Sheets `data`: `MEROS OYBEK / Chapman Brown OP 20`.
- Исправлено правило синхронизации:
  - если строка пропала из Google Sheets и по позиции нет сканов, backend помечает её как `removed_from_google_sheet` и больше не показывает в активном списке;
  - если сканы уже есть, позиция не скрывается молча, а создаётся конфликт в audit.
- Дополнительно worker умеет отправлять завершённые backend-заказы в архивный экспорт, если они всё ещё видны в `data`.

## Проверки после исправления

- Локально: `./.venv/bin/python -m unittest tests.test_google_sheets_sync_worker tests.test_google_backend_sync_diagnostic tests.test_backend_api_persistence tests.test_vds_acceptance_scripts` - OK, 44 теста.
- Локально: compileall по изменённым backend/test-файлам - OK.
- Локально: `git diff --check` - OK.
- VDS: `./deploy/vds/verify_google_backend_sync.sh` - OK, `google_rows=19`, `backend_active_items=19`, `matched_items=19`.
- VDS: `./deploy/vds/acceptance_status.sh` - OK, `google_backend_sync.status=ok`.

## VDS acceptance health retry

- Исправлен ложный failure `acceptance_status.sh` сразу после redeploy.
- Причина: контейнер `backend-api` уже `running`, но HTTP `/health` ещё может не принимать соединение.
- Теперь acceptance делает несколько попыток health-check перед финальным статусом.
- Env-настройки: `ACCEPTANCE_HEALTH_ATTEMPTS`, `ACCEPTANCE_HEALTH_RETRY_DELAY_SECONDS`.
- Проверено: локально bash/test OK, VDS `acceptance_status.sh` OK.

## SkladBot coverage gate

- Добавлен read-only verifier покрытия активных заказов номерами SkladBot.
- Если активный видимый заказ не имеет `Номер заявки SkladBot` или `ID заявки SkladBot`, VDS acceptance становится `failed`.
- Удалённые из Google строки со статусом `removed_from_google_sheet` не учитываются как активные.
- Проверено на VDS: `active_orders=7`, `numbered_orders=7`, `missing_orders=0`.
- VDS `acceptance_status.sh` теперь показывает `skladbot_coverage.status=ok`.

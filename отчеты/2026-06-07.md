## Returns/KIZ lifecycle deploy and production verification

- Задеплоен коммит `bdc08b2` на VDS через `rsync`.
- Перед deploy создан backup Postgres:
  - `/opt/taksklad/backups/postgres/taksklad-postgres-20260607T110107Z.sql.gz`.
- Применена миграция `backend/sql/002_kiz_movements.sql`.
- Пересобраны и перезапущены:
  - `backend-api`;
  - `google-sheets-sync-worker`;
  - `skladbot-worker`;
  - `telegram-worker`.
- Миграция подтверждена на боевой DB:
  - `kiz_codes=1517`;
  - `kiz_movements=1518`;
  - `uq_scan_codes_code=0`;
  - `uq_kiz_codes_code=1`.
- Полные проверки:
  - локально `python -m unittest discover tests` - 363 tests OK;
  - локально `compileall` - OK;
  - локально `npm run build` в `frontend` - OK;
  - локально `bash -n deploy/vds/*.sh` - OK;
  - локально Docker compose config для VDS/Traefik - OK;
  - локально release preflight с SHA GitHub assets - OK;
  - VDS temporary test container `python -m unittest discover tests` - 363 tests OK;
  - VDS `acceptance_status.sh` после deploy - `status=ok`;
  - публичные URL `api.taksklad.uz/health` и `taksklad.uz` отвечают 200.
- Боевой возврат `WH-R-193081`:
  - до repair заказ в DB был `returned`, но без возвратной заявки SkladBot;
  - КИЗ имел movements `outbound -> return`, latest movement `return`;
  - создано idempotent событие `skladbot_return_request_create`;
  - SkladBot создал возвратную заявку `WH-R-194284`, id `194284`;
  - событие завершено `completed`, ошибок нет;
  - Google mirror после этого обработал очередь: `pending_synced=2`, `pending_failed=0`.
- Итог:
  - DB остается основным источником;
  - Google Sheets только зеркало;
  - возвращенный КИЗ после latest movement `return` снова доступен для новой отгрузки как `re_outbound`.

# Отчёт TakSklad За 2026-05-31

## Что Сделано

- Создана локальная точка восстановления перед MVP-доработками:
  - branch: `restore/2026-05-31_before_mvp_updates_003050`;
  - tag: `restore-2026-05-31_before_mvp_updates_003050`;
  - snapshot: `/Users/anton/Documents/work/_restore_points/TakSklad_2026-05-31_before_mvp_updates_003050`.
- Зафиксированы правила нового бизнес-процесса:
  - дату отгрузки задаёт менеджер;
  - отсутствие даты отгрузки в SmartUp/Excel не считается проблемой, потому что дата задаётся вручную при импорте;
  - SkladBot сравнивается только по блокам, пачки напрямую не сравниваются;
  - товар матчится по нормализованному SKU: `brown`/`red`/`gold` и `OP`/`SSL`, даже если название в SkladBot длиннее;
  - адрес не блокирует SkladBot matching;
  - логистический отчёт должен содержать именно координаты, а не только адрес;
  - базовая цена блока: `240000` сум.
- Telegram worker переведён на нижнее меню:
  - `Дата отгрузки`;
  - `Отчёт логистики`;
  - `КИЗ по файлам`.
- Excel import расширен:
  - поддерживает координаты;
  - поддерживает суммы из файла;
  - если суммы нет, считает по `Кол-во блок * 240000`;
  - сохраняет `Кол-во ШТ` и `Кол-во блок`;
  - применяет дату отгрузки из Telegram.
- Добавлен логистический отчёт по выбранной дате:
  - `GET /api/v1/logistics/dates`;
  - `GET /api/v1/logistics/report`;
  - отчёт формируется одним Excel-файлом;
  - в отчёт попадают координаты и широта/долгота.
- SkladBot matcher обновлён:
  - учитывает только `3PL отгрузка`;
  - не принимает `Возврат 3PL` как отгрузку;
  - сравнивает дату, клиента, оплату, товары и блоки;
  - адрес не является жёстким условием.
- Добавлен отчёт `КИЗ по файлам`:
  - `GET /api/v1/reports/kiz/source-files`;
  - `GET /api/v1/reports/kiz/source-file`;
  - Telegram показывает только полностью завершённые исходные файлы.

## Проверки

- `py_compile` backend-модулей - успешно.
- `python -m unittest tests.test_backend_telegram_import tests.test_backend_api_persistence tests.test_backend_skladbot_worker` - 22 теста OK.
- `python -m unittest discover -s tests` - 74 теста OK.
- Проверены реальные sample Excel-файлы из Telegram на чтение:
  - `заказы 29.05 3 часть.xlsx` - 27 строк;
  - `заказы 29.05. 2 часть.xlsx` - 41 строка;
  - `Шаблон_отправки_заказов_на_склад_26_05_2026_2ч.xlsx` - 21 строка;
  - `Шаблон_отправки_заказов_на_склад_26_05_2026_1ч.xlsx` - 13 строк;
  - `Шаблон_отправки_заказов_на_склад_26_05_2026_1ч_терминал.xlsx` - 23 строки.

## VDS Smoke

- Создана серверная точка восстановления:
  - `/opt/taksklad/restore_points/server_20260530T194938Z/app-files.tar.gz`;
  - backup Postgres: `/opt/taksklad/backups/postgres/taksklad-postgres-20260530T194941Z.sql.gz`.
- Backend-код выложен на VDS.
- Пересобраны Docker images:
  - `backend-api`;
  - `telegram-worker`;
  - `skladbot-worker`.
- Во время обновления Telegram/SkladBot workers были остановлены, чтобы не обработать реальные сообщения в середине выкладки.
- Backend API поднят заново.
- Через backend-контейнер выполнен smoke:
  - создан тестовый импорт `SMOKE_MVP_20260531_0052.xlsx`;
  - создан тестовый заказ;
  - принято 2 тестовых КИЗа;
  - заказ завершён;
  - логистический отчёт по дате `2026-05-31` сформирован;
  - КИЗ-отчёт по исходному файлу сформирован;
  - тестовые строки удалены из Postgres.
- Проверка очистки: тестовых заказов и импортов `SMOKE_MVP_20260531_0049` / `SMOKE_MVP_20260531_0052` не осталось.
- Внешний защищённый endpoint `https://api.135.181.245.84.sslip.io/api/v1/logistics/dates` вернул `200`.
- Telegram token проверен через `getMe`; бот отвечает как `SkladKis_bot`.
- Telegram menu обновлено командами:
  - `date`;
  - `logistics`;
  - `kiz_files`.
- SkladBot one-shot worker запущен на VDS:
  - API SkladBot ответил `200`;
  - активных backend-заказов на момент проверки не было;
  - результат: `requests=0 orders=0 matched=0 not_found=0 multiple=0`.
- Исправлена причина Telegram `getUpdates` timeout: polling timeout был больше HTTP timeout клиента.
- Добавлен отдельный `TELEGRAM_WORKER_POLL_TIMEOUT_SECONDS=15`.
- Выполнен Telegram file smoke:
  - тестовый Excel загружен в Telegram через Bot API;
  - worker скачал файл по реальному `file_id`;
  - импорт применил дату отгрузки `2026-05-31`;
  - тестовые данные очищены;
  - проверка очистки: `tg_smoke_orders=0`, `tg_smoke_imports=0`, `telegram_pending=0`.
- SkladBot worker доработан:
  - не ходит в SkladBot API без активных backend-заказов;
  - обрабатывает `429 Too Many Requests` с задержкой и повтором;
  - фильтрует заявки по `unloading_date`, а не только по `created_at`.
- Выполнен SkladBot real-match smoke на существующей заявке:
  - `request_number=WH-R-190960`;
  - `request_id=190961`;
  - тип: `Отгрузка 3PL`;
  - дата выгрузки: `2026-05-29`;
  - клиент: `NICE SHOP`;
  - товар: `Chapman Brown OP 20`;
  - количество: `1` блок;
  - результат: `requests=1 orders=1 matched=1 not_found=0 multiple=0`.
- После smoke тестовые данные удалены: `orders_total=0`, `smoke_skladbot_orders=0`, `smoke_skladbot_imports=0`, `telegram_pending=0`.
- Workers снова запущены:
  - `telegram-worker`;
  - `skladbot-worker`.

## Что Не Делалось

- Входящий Excel не отправлялся в Telegram от пользовательского аккаунта. Через Bot API проверен реальный файл и `file_id`, но не пользовательское входящее сообщение.
- Заявки в SkladBot не создавались.
- Desktop-релиз и Windows-архив не собирались.
- `version.json` не повышался.
- Push-уведомления рабочим компьютерам не отправлялись.

## Остаток

1. Отправить Excel в Telegram-бот от реального пользовательского аккаунта и проверить входящую очередь.
2. Провести Windows desktop-приёмку: запуск, обновление, выбор заказа, сканирование, завершение заказа, печать, завершение смены.
3. После подтверждения - включить это в релизную ветку и отдельно решить вопрос desktop/Windows-архива.

## Контрольный Прогон После Уточнения Рисков

- Зафиксированы финальные решения по рискам:
  - Smartup/Excel без даты отгрузки закрывается ручным выбором даты менеджером в Telegram;
  - SkladBot сравнивается только по блокам;
  - длинные названия товаров SkladBot нормализуются до цвета и формата;
  - адрес остаётся мягким критерием;
  - логистический отчёт требует координаты.
- Повторно проверено текущее рабочее дерево:
  - `.venv/bin/python -m unittest discover -s tests` - 74 теста OK;
  - `.venv/bin/python -m py_compile backend/app/*.py tests/*.py` - OK;
  - `git diff --check` - OK;
  - `npm run build` в `frontend/` - OK;
  - `docker compose --env-file deploy/vds/.env.example -f deploy/vds/docker-compose.yml config` - OK.
- Быстрый поиск секретов в рабочих файлах показал только placeholder/env-названия, без реальных токенов и паролей.
- Создан локальный checkpoint текущего результата:
  - tag: `checkpoint-2026-05-31_mvp-telegram-logistics-skladbot`.
- В GitHub отправлены:
  - branch: `feature/mvp-telegram-logistics-skladbot`;
  - tag: `restore-2026-05-31_before_mvp_updates_003050`;
  - tag: `checkpoint-2026-05-31_mvp-telegram-logistics-skladbot`.
- Открыт draft PR для review/acceptance без релиза: `https://github.com/1fear/TakSklad/pull/1`.
- GitHub checks для ветки пустые: обычный push не запускает Windows release workflow.
- GitHub Release, Windows workflow и `version.json` не запускались.
- Проверено текущее состояние VDS после checkpoint:
  - `backend-api`, `frontend`, `postgres`, `telegram-worker`, `skladbot-worker` работают;
  - server restore `/opt/taksklad/restore_points/server_20260530T194938Z` на месте;
  - Postgres backup `taksklad-postgres-20260530T194941Z.sql.gz` на месте;
  - `https://api.135.181.245.84.sslip.io/health` вернул `200`;
  - `https://app.135.181.245.84.sslip.io/` без basic-auth вернул `401`, доступ закрыт.
- Проверены VDS логи workers после простоя:
  - SkladBot worker корректно пишет `no active backend orders, skip SkladBot API`;
  - новых падений Telegram worker в проверенном окне не видно;
  - backend отвечает на smoke/API запросы;
  - один `401 Unauthorized` в backend-логе ожидаем для запроса без service token.

## Web Frontend UI Smoke На VDS

- Создан временный заказ `WEB_UI_SMOKE_20260531_0118` через backend API:
  - 2 позиции;
  - 3 блока всего;
  - дата отгрузки `2026-05-31`.
- Через web-frontend `https://app.135.181.245.84.sslip.io/` выполнен полный UI-путь:
  - открыт интерфейс через basic-auth;
  - найден заказ через поиск;
  - выбрана позиция `Chapman Brown OP 20`;
  - записаны 2 КИЗа;
  - выбрана позиция `Chapman Gold SSL 20`;
  - записан 1 КИЗ;
  - заказ завершён кнопкой `Завершить`;
  - после завершения заказ пропал из активных.
- Перед очисткой проверено состояние БД:
  - order status: `completed`;
  - `Chapman Brown OP 20`: `2/2`, status `completed`;
  - `Chapman Gold SSL 20`: `1/1`, status `completed`.
- Smoke-данные удалены:
  - `orders=0`;
  - `imports=0`;
  - `import_files=0`;
  - `pending_events=0`.
- Скриншоты проверки сохранены локально во временной папке `/tmp/taksklad-web-ui-smoke/`.

## Completion Audit

- Добавлен отдельный аудит готовности: [goal-completion-audit.md](/Users/anton/Documents/work/TakSklad/docs/goal-completion-audit.md).
- Вывод аудита:
  - VDS/MVP-часть технически проверена;
  - recovery/checkpoint/PR сделаны;
  - Telegram file API проверен через реальный `file_id`;
  - SkladBot matching проверен на существующей заявке `WH-R-190960`;
  - web-frontend отпикивание проверено;
  - полное закрытие цели пока нельзя честно поставить без входящего Telegram-файла от пользовательского аккаунта и Windows desktop-приёмки.

## Manual Acceptance Pack

- Создан готовый Excel для ручного Telegram-теста:
  - `/Users/anton/Documents/work/TakSklad/outputs/taksklad_acceptance/TakSklad_Telegram_Acceptance_2026-05-31.xlsx`;
  - клиент `ACCEPTANCE TELEGRAM 20260531`;
  - дата отгрузки `31.05.2026`;
  - 2 позиции, 3 блока;
  - координаты `41.311081, 69.240562`;
  - цена блока `240000`.
- Файл проверен backend parser:
  - строк импорта `2`;
  - дата `31.05.2026`;
  - блоки `2` и `1`;
  - суммы `480000` и `240000`.
- Добавлен manual runbook: [manual-acceptance-runbook.md](/Users/anton/Documents/work/TakSklad/docs/manual-acceptance-runbook.md).
- Добавлен безопасный VDS cleanup-скрипт для acceptance-маркеров:
  - `deploy/vds/cleanup_acceptance_marker.sh`;
  - по умолчанию работает в dry-run;
  - удаляет только с `--apply`;
  - принимает только маркеры с `ACCEPTANCE`, `WEB_UI_SMOKE` или `SMOKE_MVP`.
- Проверки cleanup-скрипта:
  - `bash -n deploy/vds/cleanup_acceptance_marker.sh` - OK;
  - небезопасный marker `BAD_MARKER` отклонён;
  - VDS dry-run по `ACCEPTANCE TELEGRAM 20260531` вернул нули по `orders/imports/import_files/pending_events/audit_log`.

## Финальная Фиксация Рисков Chapman-Процесса

- Smartup/Excel без отдельного файла отгрузки не является риском: дату отгрузки выбирает менеджер в Telegram.
- SkladBot сравнивается только по блокам; Excel-пачки/штуки приводятся к `Кол-во блок`.
- Товар сравнивается по цвету `brown`/`red`/`gold` и формату `OP`/`SSL`, длинные названия SkladBot нормализуются.
- Адрес используется мягко, как дополнительный признак и диагностика, но не как главный блокирующий критерий.
- Логистический отчёт должен содержать координаты доставки.
- Эти правила добавлены в:
  - `docs/project-knowledge-base.md`;
  - `docs/project-architecture.md`;
  - `docs/product-mvp-2.0-plan.md`.
- Контрольные проверки после фиксации:
  - 74 unit-теста OK;
  - `py_compile` OK;
  - `git diff --check` OK;
  - frontend build OK;
  - VDS compose config OK;
  - shell syntax deploy/backup/restore/cleanup скриптов OK.

## Доработка После Финального Брифа Chapman

- Убрано жёсткое сравнение адреса из desktop SkladBot matching.
- Тип заявки SkladBot теперь принимается в вариантах `Отгрузка 3PL` и `3PL отгрузка`.
- Яндекс Геокодер в desktop больше не возвращает адрес со страной `Узбекистан` в начале.
- Логистический backend-отчёт требует координаты и не отдаёт пустой маршрутный файл.
- Координаты вида `41.214609,69.223027,15` приводятся к `41.214609,69.223027`.
- В КИЗ-отчёт по исходному файлу добавлен лист `Сводка` с суммой заказа, планом и фактом блоков.
- Проверены реальные Excel-шаблоны из Telegram:
  - 5 файлов импортируются;
  - строки читаются без предупреждений;
  - координаты есть во всех строках;
  - блоки и суммы считаются.
- Контрольные проверки:
  - 79 unit-тестов OK;
  - `py_compile` OK;
  - `git diff --check` OK;
  - frontend build OK;
  - VDS compose config OK.
- VDS smoke после деплоя:
  - импортирован временный заказ `SMOKE_MVP_CHAPMAN_20260531_0154`;
  - логистический отчёт отдал координаты без третьего компонента;
  - через API записаны 3 КИЗа;
  - КИЗ-отчёт по файлу содержит лист `Сводка`, 3/3 блока, сумма `720000`;
  - smoke-данные удалены cleanup-скриптом;
  - backend health вернул `200`, сервисы VDS работают.

## Пост-Чек После Финального Push

- Branch и checkpoint-тег обновлены до `bce4f8a`.
- `version.json`, Windows-архив и GitHub Release не трогались.
- VDS health: `200`.
- VDS-сервисы `backend-api`, `frontend`, `postgres`, `skladbot-worker`, `telegram-worker` находятся в состоянии `running`.
- Dry-run cleanup по `ACCEPTANCE TELEGRAM 20260531` и `SMOKE_MVP_CHAPMAN_20260531_0154` показал `0` тестовых строк.
- Свежие логи backend без ошибок после smoke.
- `skladbot-worker` корректно пропускает SkladBot API, когда нет активных backend-заказов.

Остаётся неподтверждённым:

- реальная отправка Excel-файла в Telegram-бота от разрешённого пользователя;
- физическая Windows-приёмка desktop-приложения с backend flags.

## Повторяемый VDS Smoke-Скрипт

- Добавлен `deploy/vds/smoke_mvp_chapman.sh`.
- Скрипт проверяет backend-процесс на VDS без реальных заказов:
  - импорт тестового заказа;
  - логистический отчёт с координатами;
  - запрет досрочного завершения заказа;
  - 3 скана КИЗ;
  - запрет дубля КИЗа;
  - завершение заказа;
  - КИЗ-сводка по исходному файлу;
  - автоматический cleanup.
- Запуск на VDS прошёл успешно:
  - marker `SMOKE_MVP_CHAPMAN_20260530T210739Z`;
  - импорт `2` строки, `1` заказ;
  - logistics `2` строки;
  - KIZ scans `3`;
  - duplicate scan rejected;
  - order completed;
  - KIZ summary total `720000`;
  - cleanup remaining `0`.
- Проверки после добавления:
  - `bash -n deploy/vds/*.sh` - OK;
  - `79` unit-тестов OK;
  - VDS compose config OK.

## Усиление Автотестов Desktop Backend Bridge

- Добавлены проверки очереди backend-событий desktop-приложения:
  - pending scan не дублируется;
  - pending code блокирует повторный ввод КИЗа;
  - undo удаляет pending scan;
  - pending `order_complete` отправляется в backend;
  - неизвестное событие не держит очередь.
- Проверки:
  - `tests.test_backend_bridge` - 7 тестов OK;
  - полный набор - 83 теста OK;
  - `py_compile` OK;
  - `git diff --check` OK.

## Read-Only Acceptance Verifier

- Добавлен `deploy/vds/verify_acceptance_marker.sh`.
- Назначение: быстро проверить результат ручного Telegram/Windows acceptance по маркеру без изменений в базе.
- Проверяет:
  - заказы;
  - позиции;
  - план блоков;
  - сканы;
  - imports;
  - pending events;
  - source files;
  - missing coordinates;
  - incomplete items.
- Поддерживает ожидания:
  - `--expect-orders N`;
  - `--expect-scans N`;
  - `--expect-completed`.
- Проверено на VDS:
  - пустой `ACCEPTANCE TELEGRAM 20260531` вернул `status=ok`, все счётчики `0`;
  - smoke `SMOKE_MVP_CHAPMAN_20260530T211424Z` до cleanup вернул `orders=1`, `scan_codes=3`, `completed_orders=1`, `status=ok`;
  - cleanup после smoke вернул остаток `0`.

## Генератор Acceptance Excel

- Добавлен `tools/generate_acceptance_excel.py`.
- Добавлен тест `tests/test_acceptance_excel_generator.py`.
- Пересобран tracked-файл `outputs/taksklad_acceptance/TakSklad_Telegram_Acceptance_2026-05-31.xlsx`.
- Содержимое файла:
  - клиент `ACCEPTANCE TELEGRAM 20260531`;
  - дата отгрузки `31.05.2026`;
  - 2 позиции;
  - 3 блока;
  - координаты `41.311081, 69.240562`;
  - сумма `720000`.
- Backend parser прочитал файл без предупреждений.
- Полный набор тестов: 84 OK.

## Windows Backend Acceptance Helper

- Добавлен `tools/windows_backend_acceptance.ps1`.
- Скрипт нужен для тестовой Windows-приёмки без релиза и без изменения `version.json`.
- Что делает:
  - проверяет VDS `/health`;
  - проверяет `/api/v1/orders/active` с service token;
  - запускает `TakSklad.exe` или `main.py` с backend flags;
  - умеет `-CheckOnly` без запуска приложения;
  - умеет `-Clear` для быстрого отката env.
- Секреты в скрипт не добавлены.
- Проверки:
  - `tests.test_windows_acceptance_helper` - 2 теста OK;
  - полный набор unit-тестов - 86 OK;
  - `py_compile` - OK;
  - `git diff --check` - OK.
- PowerShell runtime на macOS не установлен, поэтому финальную проверку `.ps1` нужно сделать на Windows.

## Acceptance Kit Для Ручной Проверки

- Добавлен `tools/prepare_acceptance_kit.py`.
- В `outputs/taksklad_acceptance/` теперь лежит полный набор для ручной проверки:
  - `TakSklad_Telegram_Acceptance_2026-05-31.xlsx`;
  - `acceptance_manifest.json`;
  - `README.md`.
- Manifest фиксирует:
  - marker `ACCEPTANCE TELEGRAM 20260531`;
  - дату отгрузки `31.05.2026`;
  - 2 строки, 2 позиции, 3 блока;
  - сумму `720000`;
  - 3 тестовых КИЗа;
  - команды проверки Telegram/Windows/VDS;
  - SHA-256 Excel.
- Excel сделан с повторяемым SHA-256:
  - `a5abc62efebcd2d87e26e92dfbb990d22fbf72e86ae74914b0dbf9b6f8de285e`.
- Проверки:
  - `tools/prepare_acceptance_kit.py` - OK;
  - повторная генерация дала тот же SHA-256;
  - `tests.test_acceptance_excel_generator` - 3 OK;
  - полный набор unit-тестов - 88 OK;
  - `py_compile` - OK.

## Wait Acceptance Verifier

- Добавлен `deploy/vds/wait_acceptance_marker.sh`.
- Скрипт нужен, чтобы после ручной отправки Excel в Telegram или Windows-сканов не запускать verifier вручную каждые несколько секунд.
- Он ждёт, пока `verify_acceptance_marker.sh` вернёт OK, либо завершает ожидание по timeout.
- Команды добавлены в acceptance kit:
  - `telegram_wait`;
  - `windows_wait`.
- Проверки:
  - `bash -n deploy/vds/*.sh` - OK;
  - `wait_acceptance_marker.sh --help` - OK;
  - небезопасный marker отклоняется сразу;
  - manifest/README пересобраны;
  - тесты acceptance kit проверяют наличие wait-команд.

## VDS Acceptance Kit Sync

- Acceptance kit и wait-verifier загружены на VDS в `/opt/taksklad/app`.
- Обновлены только acceptance-файлы, скрипты и документация.
- Не трогались:
  - `.env`;
  - Postgres;
  - контейнеры;
  - `version.json`;
  - release/archive/update flow.
- Проверки на VDS:
  - `bash -n deploy/vds/*.sh` - OK;
  - `wait_acceptance_marker.sh --help` - OK;
  - `BAD_MARKER` отклоняется с exit `2`;
  - `wait_acceptance_marker.sh "ACCEPTANCE TELEGRAM 20260531" --timeout 5 --interval 1` - OK;
  - `verify_acceptance_marker.sh "ACCEPTANCE TELEGRAM 20260531"` - OK, текущие тестовые данные отсутствуют;
  - Excel SHA-256 на VDS: `a5abc62efebcd2d87e26e92dfbb990d22fbf72e86ae74914b0dbf9b6f8de285e`;
  - backend health - OK;
  - VDS `version.json` остался на `1.1.7`.

## Acceptance Status Check

- Добавлен `deploy/vds/acceptance_status.sh`.
- Скрипт нужен как единая read-only проверка перед/после ручного Telegram и Windows acceptance.
- Проверяет:
  - manifest;
  - SHA-256 Excel;
  - `version.json`;
  - Docker services;
  - backend health;
  - acceptance marker.
- Команды добавлены в acceptance kit:
  - `vds_status`;
  - `telegram_status`;
  - `windows_status`.
- Проверки:
  - `bash -n deploy/vds/*.sh` - OK;
  - `acceptance_status.sh --help` - OK;
  - тесты acceptance kit проверяют наличие status-команд.

## VDS Acceptance Status Check

- Новый `acceptance_status.sh` загружен на VDS и проверен.
- Результат на VDS: `status=ok`.
- Проверено:
  - Excel SHA-256 совпадает с manifest;
  - backend health - OK;
  - Docker services running;
  - acceptance marker пока пустой;
  - `version.json` остался `1.1.7`, `mandatory=false`, download URL пустой.
- Был один временный SSH timeout после `rsync`; повторная проверка прошла успешно, backend по HTTPS отвечал OK.

## TakSklad 2.0: интерфейс, Telegram, SkladBot и VDS-домен

Зачем:

- привести desktop-интерфейс к утверждённому рабочему экрану склада;
- убрать старые пользовательские Telegram-кнопки, которые уже не нужны;
- не допустить пропуска SkladBot-заявок на будущие даты отгрузки;
- переключить backend с временного `sslip.io` на нормальный домен `api.taksklad.uz`;
- не трогать `version.json` и не запускать автообновления рабочих ПК.

Что сделано:

- основной складской экран теперь показывает `Заказы для КИЗов`;
- заказы в списке разделяются по датам отгрузки;
- видимые кнопки `Импорт Excel`, `Товары`, `Контроль` убраны с рабочего экрана склада;
- добавлена кнопка `Возвраты`;
- добавлено окно возвратов: скан/ввод номера заявки SkladBot, поиск закрытой заявки, подтверждение возврата;
- возврат в backend ставит статус `returned`, дату возврата и audit log;
- кнопка отчёта переименована в `Закрыть смену`;
- закрытие смены формирует КИЗ-файлы отдельно по датам отгрузки;
- если по одной дате закрывают частями, файлы получают `ч1`, `ч2` и далее;
- старый авто-дневной таймер desktop отключён, чтобы не было дублей;
- печать остаётся только после `Завершить заказ`, отдельной кнопки печати на экране склада нет;
- кнопки desktop переведены на округлённый вид и цвета TakSklad;
- Telegram worker оставлен с нижним меню:
  - `Дата отгрузки`;
  - `Отчёт логистики`;
  - `КИЗ по файлам`;
- Telegram `/report` убран из пользовательского workflow;
- `/health` и `/imports` оставлены только как скрытый админский fallback;
- SkladBot worker теперь проверяет свежесть заявки по `created_at/updated_at`, а не по `Дата выгрузки`;
- `Дата выгрузки` SkladBot остаётся строгим полем для совпадения с датой отгрузки заказа;
- SkladBot worker не отбрасывает заявку до detail, если в list response нет поля `type`;
- интервал SkladBot worker уменьшен до 60 секунд;
- `api.taksklad.uz` переключён на VDS, HTTPS health работает;
- backend/deploy-изменения загружены на VDS;
- пересобраны и перезапущены `backend-api`, `telegram-worker`, `skladbot-worker`.
- добавлена read-only диагностика SkladBot matching:
  - `backend/app/skladbot_diagnostic.py`;
  - `deploy/vds/diagnose_skladbot_match.sh`;
  - показывает причины несовпадения `date/client/payment/products`.

Проверки:

- локально targeted tests: 29 тестов OK;
- реальные Excel-шаблоны из Telegram разобраны, блоки и суммы считаются корректно;
- `https://api.taksklad.uz/health` вернул `ok`;
- VDS `acceptance_status.sh` вернул `status=ok`;
- Telegram API `getMyCommands` вернул только `date`, `logistics`, `kiz_files`;
- `diagnose_skladbot_match.sh --help` проверен;
- VDS `smoke_mvp_chapman.sh` прошёл: импорт, сканы, дубль, завершение заказа, логистика, КИЗ summary и cleanup.

Что не получилось/не закрыто:

- не выполнялся реальный Telegram upload test через боевой чат;
- SkladBot matching не проверен на новой живой активной заявке `3PL отгрузка`;
- Windows-приёмка desktop с backend flags, сканером и принтером ещё не пройдена;
- Windows archive и `version.json` не менялись.

## SkladBot Diagnostic Limit

- Read-only диагностика SkladBot matching дополнительно ограничена:
  - если по маркеру нет активных backend-заказов, она не обращается к SkladBot API;
  - добавлен параметр `--request-limit`, чтобы acceptance-проверка не проходила по большому списку заявок;
  - команда в runbook обновлена до `--limit 5 --request-limit 20`.
- Правка загружена на VDS и проверена:
  - `https://api.taksklad.uz/health` - OK;
  - `acceptance_status.sh` - `status=ok`;
  - `diagnose_skladbot_match.sh --marker "ACCEPTANCE TELEGRAM 20260531" --limit 5 --request-limit 20` вернул `active_orders=0`, `candidate_requests=0`;
  - зависших процессов диагностики на VDS не осталось.

## Desktop Print Window Sizes

- Окно печати сводного листа обновлено:
  - показывает доступные системные принтеры, если ОС отдаёт список;
  - поддерживает размеры этикеток `100x100`, `100x150`, `75x50`, `58x40`;
  - сохраняет выбранный принтер и размер;
  - `Enter` подтверждает печать, `Esc` отменяет.
- Печать остаётся прямой через ОС: браузер для сводного листа не открывается.
- Добавлен тест `tests.test_printing`: проверяет парсер размеров и фактический размер PNG для выбранной этикетки.

## Backend Diagnostics Logs

- Добавлен endpoint `GET /api/v1/diagnostics/logs`.
- Endpoint формирует текстовый diagnostic-файл:
  - failed/error события очередей;
  - импорты со статусами `failed` и `completed_with_errors`;
  - последние служебные audit-события `orders_imported`, `skladbot_worker_sync`, `order_returned`.
- Обычные события сканирования КИЗов не попадают в файл, чтобы не засорять его складскими дублями и кодами.
- Очевидные токены/секреты в тексте маскируются.
- В Telegram добавлена скрытая команда `/logs`, которая отправляет этот файл. Нижнее пользовательское меню не изменилось.
- Покрыто тестами:
  - `test_diagnostics_logs_include_failed_events_import_errors_and_redact_secrets`;
  - `test_telegram_worker_handles_hidden_logs_command`.
- Проверено:
  - локально `97` тестов OK;
  - `py_compile` OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` OK;
  - VDS пересобран с `backend-api`, `telegram-worker`, `skladbot-worker`;
  - `https://api.taksklad.uz/health` - OK;
  - `acceptance_status.sh` - `status=ok`;
  - `/api/v1/diagnostics/logs` на VDS вернул `200` и файл `TakSklad_backend_diagnostics_*.txt`.

## Yandex Geocoder Secret Cleanup

- Удалён зашитый ключ Яндекс Геокодера из `src/taksklad/config.py`.
- Геокодинг теперь берёт ключ только из env `YANDEX_GEOCODER_API_KEY` или локального `yandex_geocoder_key.txt`.
- Локальный файл ключа уже исключён из Git через `.gitignore`.
- Добавлены тесты `tests/test_geocoding.py`.
- Проверки:
  - `tests.test_geocoding` - 3 теста OK;
  - полный `unittest discover -s tests` - 100 тестов OK;
  - `py_compile` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK.
- Что осталось: перед боевым релизом ротировать старый ключ и положить новый на Windows-тестовую копию через env или локальный файл.

## Desktop Log Rotation

- Добавлена ротация `docs/TakSklad.log`.
- Основной лог ограничен `5 MB`, хранится до `5` архивов.
- Настройки можно менять через env:
  - `TAKSKLAD_LOG_MAX_BYTES`;
  - `TAKSKLAD_LOG_BACKUP_COUNT`.
- Добавлен модуль `src/taksklad/logging_setup.py`.
- Добавлены тесты `tests/test_logging_setup.py`.
- Проверки:
  - `tests.test_logging_setup tests.test_geocoding` - 5 тестов OK;
  - полный `unittest discover -s tests` - 102 теста OK;
  - `py_compile` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK.

## Desktop Startup Self-Check

- Добавлен `src/taksklad/startup_check.py`.
- При запуске desktop пишет `Startup self-check` в лог.
- Self-check фиксирует безопасный fingerprint конфигурации:
  - версия;
  - hash Google spreadsheet id;
  - имя листа;
  - статус credentials;
  - Telegram enabled/token/chat count;
  - backend flags/origin/token-present;
  - наличие ключа Яндекс Геокодера;
  - размеры локальных очередей.
- Секреты, token, private key, chat_id, КИЗы и полный spreadsheet id не логируются.
- Добавлены тесты `tests/test_startup_check.py`.
- Проверки:
  - `tests.test_startup_check tests.test_logging_setup tests.test_geocoding` - 7 тестов OK;
  - полный `unittest discover -s tests` - 104 теста OK;
  - `py_compile` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK.

## Desktop Refresh Diagnostic Summary

- Добавлен `src/taksklad/desktop_diagnostics.py`.
- После успешного обновления списка заказов desktop пишет `Refresh diagnostic summary`.
- Summary показывает только счётчики:
  - источник `google/backend`;
  - активные строки, группы, даты отгрузки;
  - известные КИЗы;
  - размеры очередей;
  - итоги локальной, backend и SkladBot-синхронизации.
- Клиенты, адреса, товары, КИЗы и payload очередей не логируются.
- Добавлен тест `tests/test_desktop_diagnostics.py`.
- Проверки:
  - `tests.test_desktop_diagnostics tests.test_startup_check tests.test_logging_setup tests.test_geocoding` - 8 тестов OK;
  - полный `unittest discover -s tests` - 105 тестов OK;
  - `py_compile` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK.

## Windows Acceptance Helper DNS/Version Guard

- Исправлен helper `tools/windows_backend_acceptance.ps1` для Windows-приёмки:
  - основной backend URL переключён на `https://api.taksklad.uz`;
  - добавлен `-UsePython`, чтобы запускать текущие исходники даже если рядом лежит exe;
  - при запуске `main.py` проверяется `APP_VERSION = 1.1.17`;
  - helper предпочитает `.venv\Scripts\python.exe`;
  - для exe выводится предупреждение, что старый ярлык `1.1.7` использовать нельзя.
- Обновлены Windows acceptance runbook, manual acceptance runbook, deploy/rollback runbook и acceptance kit.
- Проверки:
  - `tests.test_windows_acceptance_helper tests.test_acceptance_excel_generator` - 5 тестов OK;
  - полный `unittest discover -s tests` - 105 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён.
- Не получилось проверить PowerShell parser локально: на macOS нет `pwsh`. Это нужно проверить на Windows при приёмке.

## Windows Test Archive Helper

- Добавлен `tools/build_windows_test_archive.ps1`.
- Это безопасная сборка test archive для Windows-приёмки:
  - собирает PyInstaller `--onedir`;
  - проверяет, что `version.json` не менялся;
  - требует стабильный публичный `version.json = 1.1.7`;
  - добавляет acceptance helper и acceptance kit;
  - проверяет наличие `TakSklad\TakSklad.exe`;
  - не даёт собрать пакет, если внутрь попали `TakSklad_data.json`, `credentials.json`, `telegram_settings.json`, `yandex_geocoder_key.txt` или `pending_*.json`;
  - создаёт ZIP и `.sha256.txt` в `outputs\windows_test_build`;
  - не создаёт GitHub Release и не запускает автообновление.
- Обновлены acceptance docs и acceptance kit.
- Проверки:
  - `tests.test_windows_test_build_helper tests.test_acceptance_excel_generator tests.test_windows_acceptance_helper` - 7 тестов OK;
  - полный `unittest discover -s tests` - 107 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён.
- На macOS сам Windows archive не собирался. Скрипт нужно запускать на Windows.

## Local Release Preflight

- Добавлен `tools/release_preflight.py`.
- Это локальная проверка перед ручной приёмкой:
  - наличие нужных helper/runbook-файлов;
  - `version.json` закреплён на `1.1.7`, без download URL и без git diff;
  - acceptance Excel совпадает с SHA256 из manifest;
  - acceptance marker содержит `ACCEPTANCE`;
  - safety-флаги acceptance kit выставлены;
  - runtime/secret-файлы не отслеживаются Git;
  - `https://api.taksklad.uz/health` отвечает `status=ok`.
- Добавлены тесты `tests/test_release_preflight.py`.
- Проверки:
  - `tests.test_release_preflight tests.test_acceptance_excel_generator` - 7 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`, `https://api.taksklad.uz/health` ответил `status=ok`;
  - `py_compile` для `tools/release_preflight.py` - OK.
  - полный `unittest discover -s tests` - 111 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён.

## Acceptance Result Template

- В acceptance kit добавлен `ACCEPTANCE_RESULTS_TEMPLATE.md`.
- Шаблон нужен, чтобы фиксировать фактические результаты приёмки, а не закрывать релиз на словах:
  - preflight;
  - Telegram import;
  - SkladBot matching;
  - Windows desktop acceptance;
  - cleanup;
  - defects/known issues;
  - итог `GO/NO-GO`.
- `tools/release_preflight.py` теперь проверяет наличие этого шаблона.
- Проверки после последней правки:
  - `tests.test_release_preflight tests.test_acceptance_excel_generator` - 8 тестов OK;
  - `.venv/bin/python tools/release_preflight.py --skip-network` - `status=ok`;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`, `https://api.taksklad.uz/health` ответил `status=ok`;
  - полный `unittest discover -s tests` - 112 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён, push-обновления рабочим ПК не включались.

## Logistics Report Blocks And Prices

- Проверен реальный шаблон логистики `Список_заказов_на_доставку_Чапамана_на_29_05_2026.xlsx`.
- Найден риск: backend мог выводить в логистический отчёт количество в пачках/штуках и цену за штуку.
- Исправлено:
  - `Кол-во` в логистическом Excel теперь берётся из блоков;
  - `Цена` теперь цена за блок;
  - fallback цена за блок - `240000`;
  - `Цена заказа` остаётся общей суммой.
- Пример после исправления: Smartup `200` пачек -> логистика `20` блоков, цена `240000`, сумма `4800000`.
- Проверки:
  - 3 targeted logistics tests - OK;
  - полный `unittest discover -s tests` - 112 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён.

## Returns List In Backend And Desktop

- Добавлен список возвратов:
  - backend endpoint `GET /api/v1/returns`;
  - поля `return_status`, `returned_at`, `return_reference` в ответе заказа;
  - блок `Последние возвраты` в desktop-окне `Возвраты`.
- После принятия возврата список обновляется сразу.
- Это делает вкладку возвратов понятнее складу: можно видеть, что уже принято, а не только одноразовое сообщение.
- Проверки:
  - targeted backend return test - OK;
  - полный `unittest discover -s tests` - 112 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён.

## Return Duplicate Guard And Acceptance Checklist

- Исправлен риск повторного принятия одного возврата:
  - backend возвращает `409`, если заказ уже `returned`;
  - desktop показывает, что возврат уже принят, и блокирует кнопку `Принять возврат`.
- Acceptance kit пересобран, в result template добавлены шаги проверки возвратов.
- Manual acceptance runbook дополнен проверкой:
  - открыть `Возвраты`;
  - найти завершённую заявку;
  - принять возврат;
  - увидеть её в `Последние возвраты`;
  - проверить, что повторное принятие запрещено.
- Проверки:
  - targeted backend return test - OK;
  - acceptance generator tests - OK;
  - полный `unittest discover -s tests` - 112 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`, acceptance kit SHA обновлён и совпадает с manifest;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён.

## SkladBot Vendor Code Product Matching

- Исправлен риск матчинга товаров SkladBot по vendor code без пробелов.
- Теперь backend worker и desktop fallback понимают `CHPMBrownOP20UZ` как `brown/op`, а `CHPMGoldSSL20UZ` как `gold/ssl`.
- Это важно для подтягивания номера заявки, если SkladBot отдаёт товар не красивым названием, а кодом.
- Логика остаётся строгой: цвет, формат и блоки должны совпасть.
- Проверки:
  - backend SkladBot tests - OK;
  - desktop SkladBot vendor-code regression - OK;
  - полный `unittest discover -s tests` - 114 тестов OK;
  - `py_compile` для `main.py`, `src/taksklad/*.py`, `backend/app/*.py`, `tools/*.py` - OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK;
  - `version.json` не изменён.

## Desktop UI Contract Guard

- Добавлен регрессионный тест на главный складской экран, чтобы dev-интерфейс 2.0 не откатился к старой форме `1.1.7`.
- Тест закрепляет:
  - заголовок списка `Заказы для КИЗов`;
  - кнопку `Возвраты`;
  - кнопку `Закрыть смену`;
  - отсутствие старых кнопок `Импорт Excel`, `Товары`, `Контроль` на рабочем экране склада;
  - палитру TakSklad `#F0E68C` + чёрный;
  - округлённые кнопки через `AppButton`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_desktop_ui_contract` - 3 теста OK.
  - `.venv/bin/python -m unittest discover -s tests` - 117 тестов OK.
  - `git diff --check` - OK.
  - `version.json` не изменён.

## Windows Acceptance Old Exe Guard

- Усилен Windows helper приёмки, чтобы случайно не запустить старый рабочий `TakSklad.exe` `1.1.7` вместо свежей тестовой сборки.
- Теперь при запуске `.exe` helper требует `build_manifest.json` из test archive и сверяет `app_version`.
- Если manifest нет, запуск останавливается до открытия приложения. Обойти можно только явно через `-SkipAppVersionCheck`, для намеренной ручной проверки.
- В Windows test archive добавлен `ACCEPTANCE_RESULTS_TEMPLATE.md`.
- Обновлены инструкции Windows-приёмки.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_windows_acceptance_helper tests.test_windows_test_build_helper` - 4 теста OK.
  - `.venv/bin/python -m unittest discover -s tests` - 117 тестов OK.
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`.
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK.
  - `version.json` не изменён.

## Release Preflight Windows Acceptance Guard

- Расширен `tools/release_preflight.py`: теперь он проверяет готовность Windows acceptance flow.
- Preflight ловит ситуацию, если:
  - Windows helper потерял проверку `build_manifest.json`;
  - test archive builder перестал добавлять `ACCEPTANCE_RESULTS_TEMPLATE.md`;
  - builder перестал защищать `version.json = 1.1.7`;
  - builder перестал проверять отсутствие runtime/secret-файлов в test package.
- Acceptance kit пересобран; README теперь описывает запуск exe только через свежий test archive с `build_manifest.json`.
- Исправлена стабильность acceptance Excel: повторная генерация теперь даёт одинаковый `.xlsx` byte-for-byte, включая `docProps/core.xml`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_release_preflight tests.test_acceptance_excel_generator` - 11 тестов OK.
  - текущий SHA-256 acceptance Excel: `204b932a704b39294b513a95964844db1ed74d028e3daff13beef3ab09ec98fd`.
  - `.venv/bin/python -m unittest discover -s tests` - 120 тестов OK.
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`, включая `windows_acceptance_flow`.
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK.
  - `version.json` не изменён.

## VDS Acceptance Status Rollout Guard

- Усилен `deploy/vds/acceptance_status.sh`.
- Теперь серверная read-only проверка дополнительно контролирует:
  - `result_template` из manifest существует;
  - `version.json` закреплён на `1.1.7`;
  - `mandatory=false`;
  - download URL пустые;
  - safety-флаги manifest запрещают release/push/secrets до приёмки.
- Добавлен тест `tests/test_vds_acceptance_scripts.py`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_vds_acceptance_scripts` - 2 теста OK.
  - `.venv/bin/python -m unittest discover -s tests` - 122 теста OK.
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`.
  - `bash -n deploy/vds/*.sh` и `git diff --check` - OK.
  - `version.json` не изменён.

## VDS Acceptance Kit Sync After Rollout Guard

- На VDS загружены только acceptance/preflight файлы, без остановки сервисов:
  - acceptance scripts;
  - acceptance manifest/README/result template;
  - acceptance Excel.
- Выполнен VDS read-only status:
  - `./deploy/vds/acceptance_status.sh` - `status=ok`;
  - backend health - `status=ok`;
  - `backend-api`, `frontend`, `postgres`, `skladbot-worker`, `telegram-worker` - running;
  - Excel SHA совпал: `204b932a704b39294b513a95964844db1ed74d028e3daff13beef3ab09ec98fd`;
  - acceptance marker пустой: `orders=0`, `scan_codes=0`, `pending_events=0`;
  - VDS `version.json` остался `1.1.7`, `mandatory=false`, download URL пустые.
- Не трогались:
  - `.env`;
  - Postgres data;
  - Docker containers;
  - `version.json`;
  - GitHub Release / Windows release archive / push-уведомления.

## Windows Acceptance Minimum Version Guard

- Исправлена логика проверки версии в Windows helper приёмки.
- Теперь helper принимает любую сборку не ниже `1.1.17`, поэтому будущая `2.0.0` не будет ошибочно заблокирована.
- Строгая проверка конкретной версии сохранена как опция `-ExpectedAppVersion`.
- Старый рабочий `TakSklad.exe` без `build_manifest.json` по-прежнему блокируется до запуска.
- Обновлены preflight, acceptance kit README, Windows-инструкции и тесты.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_windows_acceptance_helper tests.test_release_preflight tests.test_windows_test_build_helper` - 11 тестов OK.
  - `version.json` не изменён.

## Acceptance Kit Regeneration And VDS Status After Minimum Guard

- Acceptance kit пересобран после правки Windows helper.
- Локальные проверки:
  - `.venv/bin/python -m unittest discover -s tests` - 122 теста OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff --check` - OK;
  - `bash -n deploy/vds/*.sh` - OK;
  - `version.json` без изменений.
- На VDS повторно загружены только read-only acceptance-файлы.
- VDS status:
  - `./deploy/vds/acceptance_status.sh` - `status=ok`;
  - backend health - OK;
  - `backend-api`, `frontend`, `postgres`, `skladbot-worker`, `telegram-worker` - running;
  - acceptance Excel SHA совпал: `204b932a704b39294b513a95964844db1ed74d028e3daff13beef3ab09ec98fd`;
  - acceptance marker пустой: `orders=0`, `scan_codes=0`, `pending_events=0`;
  - VDS `version.json` остался `1.1.7`, `mandatory=false`, download URL пустые.
- Релиз, auto-update и push-уведомления не запускались.

## Telegram Update Isolation Guard

- Усилен Telegram worker для сценария нескольких файлов подряд.
- Теперь ошибка на одном update не останавливает обработку всей пачки сообщений.
- Если кнопка или отчёт временно падают из-за backend, пользователь получает понятное сообщение, а следующие сообщения продолжают обрабатываться.
- Это защищает сценарий: менеджер отправил несколько Excel-файлов, один action дал ошибку, остальные файлы всё равно попали в очередь.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_telegram_import` - 11 тестов OK.
  - `.venv/bin/python -m unittest discover -s tests` - 123 теста OK.
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`.
  - `git diff --check` - OK.
  - `version.json` не изменён.

## Import Address Country Prefix Cleanup

- Исправлена очистка адреса при backend Excel import.
- Теперь из адреса убирается не только `Узбекистан, ...`, но и `Uzbekistan, ...`, `O'zbekiston, ...`, `Oʻzbekiston, ...`.
- Это соответствует ТЗ: в логистике нужен город и адрес без страны.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_telegram_import` - 12 тестов OK.
  - `.venv/bin/python -m unittest discover -s tests` - 124 теста OK.
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`.
  - `git diff --check` - OK.
  - `bash -n deploy/vds/*.sh` - OK.
  - `version.json` не изменён.

## Backend Address Geocoding For Telegram Import

- Добавлено получение координат по адресу в backend Telegram import.
- Если Excel не содержит координаты, но содержит адрес, importer пробует Яндекс Геокодер через env `YANDEX_GEOCODER_API_KEY`.
- Ответ Яндекса приводится к формату `latitude, longitude`, который нужен логистическому отчёту.
- На один импорт есть cache адресов, чтобы одинаковые адреса не запрашивались повторно.
- В `deploy/vds/docker-compose.yml` проброшены:
  - `YANDEX_GEOCODER_API_KEY`;
  - `TAKSKLAD_DEFAULT_BLOCK_PRICE`.
- `.env.example` обновлён без секретов.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_telegram_import tests.test_vds_acceptance_scripts` - 16 тестов OK.
  - `docker compose --env-file deploy/vds/.env.example -f deploy/vds/docker-compose.yml config` - OK.
  - `.venv/bin/python -m unittest discover -s tests` - 126 тестов OK.
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`.
  - `git diff --check` - OK.
  - `bash -n deploy/vds/*.sh` - OK.
  - `version.json` не изменён.

## VDS Backend Refresh After Geocoding Update

- Актуальный backend и VDS compose доставлены на сервер без секретов и без релизного выката.
- Перезапущены только:
  - `backend-api`;
  - `telegram-worker`;
  - `skladbot-worker`.
- Не трогались:
  - Postgres data;
  - frontend;
  - `version.json`;
  - GitHub Release;
  - Windows release archive;
  - push-уведомления.
- Серверная проверка:
  - `./deploy/vds/acceptance_status.sh` - `status=ok`;
  - `https://api.taksklad.uz/health` - `200`, `status=ok`;
  - контейнеры приложения running;
  - acceptance Excel SHA совпадает: `204b932a704b39294b513a95964844db1ed74d028e3daff13beef3ab09ec98fd`;
  - серверный `version.json` остался `1.1.7`, без download URL.
- Ограничение:
  - `YANDEX_GEOCODER_API_KEY` на VDS пока пустой;
  - код геокодинга готов, но реальные координаты через Яндекс будут подтягиваться только после добавления ключа.
- Локальная проверка:
  - `.venv/bin/python -m unittest discover -s tests` - 126 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff --check` - OK;
  - `bash -n deploy/vds/*.sh` - OK;
  - `version.json` не изменён.

## Local Desktop 2.0 Source Launcher

- Добавлен `tools/run_desktop_local.sh`.
- Зачем: чтобы локально запускать именно новый desktop-интерфейс из `src/taksklad`, а не старый ярлык/старый exe `1.1.7`.
- Команда:

```bash
cd /Users/anton/Documents/work/TakSklad
./tools/run_desktop_local.sh
```

- Это не релизная Windows-сборка и не автообновление.

## Telegram Hidden Admin Commands Guard

- Добавлена настройка `TELEGRAM_ADMIN_CHAT_IDS`.
- Нижнее меню Telegram не менялось: `Дата отгрузки`, `Отчёт логистики`, `КИЗ по файлам`.
- Командное меню Telegram не содержит админские `/health`, `/imports`, `/logs`.
- Если `TELEGRAM_ADMIN_CHAT_IDS` задан, скрытые админские команды доступны только этим chat_id.
- Если `TELEGRAM_ADMIN_CHAT_IDS` пустой, прежний fallback для разрешённых chat_id сохраняется, чтобы не потерять диагностику до настройки админов.
- На VDS доставлены изменения Telegram worker и compose.
- Пересобраны `telegram-worker` и зависимый `backend-api`.
- Не трогались:
  - Postgres data;
  - frontend;
  - `version.json`;
  - Windows release archive;
  - GitHub Release;
  - push-уведомления.
- Проверка:
  - `./deploy/vds/acceptance_status.sh` на VDS - `status=ok`;
  - `https://api.taksklad.uz/health` - `200`, `status=ok`;
  - `.venv/bin/python -m unittest tests.test_backend_telegram_import tests.test_vds_acceptance_scripts` - 17 тестов OK;
  - `docker compose --env-file deploy/vds/.env.example -f deploy/vds/docker-compose.yml config` - OK;
  - `git diff --check` - OK.

## VDS Runtime Guard For Telegram Admins And SkladBot Interval

- Проверил VDS `.env` без вывода секретов.
- До правки:
  - разрешённые Telegram chat_id были заданы;
  - `TELEGRAM_ADMIN_CHAT_IDS` был пустой;
  - `SKLADBOT_WORKER_INTERVAL_SECONDS=600`;
  - `YANDEX_GEOCODER_API_KEY` пустой.
- Сделано:
  - `TELEGRAM_ADMIN_CHAT_IDS` задан текущими разрешёнными chat_id;
  - `SKLADBOT_WORKER_INTERVAL_SECONDS` снижен до `60`;
  - дефолт в `backend/app/skladbot_worker.py` также снижен до `60`;
  - `deploy/vds/.env.example` обновлён на `60`;
  - `deploy/vds/docker-compose.yml` теперь поддерживает `TAKSKLAD_ENV_FILE`, чтобы локальный `docker compose config` можно было гонять на `.env.example` без подмешивания локального `.env`.
- На VDS синхронизированы backend worker и compose-файлы.
- Контейнеры `backend-api`, `skladbot-worker`, `telegram-worker` running.
- Не трогались:
  - Postgres data;
  - frontend;
  - `version.json`;
  - Windows release archive;
  - GitHub Release;
  - push-уведомления.
- Проверка:
  - VDS `acceptance_status.sh` - `status=ok`;
  - `https://api.taksklad.uz/health` - `200`, `status=ok`;
  - clean compose config с `.env.example` показывает `SKLADBOT_WORKER_INTERVAL_SECONDS=60`.

## Release GO/NO-GO Machine Gate

- Добавлен `tools/release_go_no_go.py`.
- Зачем: чтобы перед релизом 2.0 был жёсткий машинный стоп, если ручная приёмка не заполнена или есть критичный дефект.
- Проверяет заполненный файл `outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md`.
- Требует:
  - Telegram import принят;
  - SkladBot matching принят;
  - Windows desktop acceptance принят;
  - критичных дефектов нет;
  - rollback понятен;
  - `version.json` не менялся;
  - итог `GO` отмечен, `NO-GO` не отмечен.
- Если в таблице дефектов есть незакрытый `critical`/`blocker`/`p0`/`p1`, gate возвращает `no_go`.
- Добавлено в:
  - `tools/release_preflight.py`;
  - `tools/build_windows_test_archive.ps1`;
  - `tools/prepare_acceptance_kit.py`;
  - acceptance kit.
- На VDS синхронизированы gate и acceptance kit.
- Проверка:
  - полный `unittest discover -s tests` - 138 тестов OK;
  - `release_preflight.py` - `status=ok`;
  - незаполненный acceptance template даёт `status=no_go`;
  - VDS `acceptance_status.sh` - `status=ok`;
  - `version.json` не изменён.

## Desktop Final Position Finish Flow

- Исправлен UX последней позиции заказа.
- Было: после полного скана последней позиции склад видел подсказку `Следующая позиция`.
- Стало: после полного скана последней позиции активируется `ЗАВЕРШИТЬ ЗАКАЗ`.
- Печать по-прежнему не видна на главном экране и открывается только после `ЗАВЕРШИТЬ ЗАКАЗ`.
- Для заказов с несколькими позициями промежуточные позиции работают как раньше: `Следующая позиция`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_desktop_ui_contract tests.test_printing tests.test_daily_report` - 8 тестов OK;
  - `version.json` не изменён;
  - Windows release archive и push-уведомления не запускались.

## Telegram Bottom Keyboard Regression Guard

- Усилен тест на нижнюю панель Telegram.
- Теперь проверяется:
  - кнопки `Дата отгрузки`, `Отчёт логистики`, `КИЗ по файлам`;
  - persistent keyboard;
  - keyboard остаётся при отправке Excel-документа пользователю.
- Зачем: чтобы бот не откатился к сценарию, где менеджер должен пользоваться видимыми slash-командами.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_telegram_import` - 16 тестов OK;
  - `version.json` не изменён.

## Release GO/NO-GO Section Gate

- Усилен `tools/release_go_no_go.py`.
- Теперь нельзя поставить общий `GO`, если не отмечены реальные разделы приёмки.
- Gate требует:
  - наличие разделов `Preflight`, `Telegram Import`, `SkladBot Matching`, `Windows Desktop Acceptance`, `Cleanup`, `Defects`, `Go / No-Go`;
  - все чекбоксы разделов `1-5` отмечены;
  - финальные GO-чекбоксы отмечены;
  - `NO-GO` не отмечен;
  - критичных незакрытых дефектов нет.
- Зачем: чтобы релиз 2.0 нельзя было считать готовым без фактического Telegram/SkladBot/Windows/cleanup результата.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_release_go_no_go tests.test_acceptance_excel_generator tests.test_release_preflight` - 18 тестов OK;
  - `version.json` не изменён.

## SkladBot Safe Partial Product Match

- Исправлен риск ложного `Не найдено`, когда в TakSklad есть часть товаров заказа, а в SkladBot-заявке уже полный набор товаров.
- Теперь все товары/блоки из TakSklad должны совпасть, но лишние товары в SkladBot не блокируют совпадение.
- Если по такому правилу подходит несколько заявок, номер не записывается, остаётся `multiple`.
- Пустая группа товаров не матчится.
- Изменены:
  - `backend/app/skladbot_worker.py`;
  - `src/taksklad/skladbot.py`;
  - backend/desktop тесты SkladBot.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_skladbot_worker tests.test_skladbot_sync` - 30 тестов OK;
  - `.venv/bin/python -m unittest discover -s tests` - 142 теста OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff --check` - OK;
  - `tools/release_go_no_go.py` на незаполненном acceptance template вернул `status=no_go` и exit code `3`, как и должен до ручной приёмки;
  - `version.json` не изменён.

## VDS SkladBot Worker Sync After Partial Match

- Проверил VDS после локальной правки partial-match.
- До синхронизации `backend/app/skladbot_worker.py` на VDS отличался от локальной версии.
- На VDS доставлен только `backend/app/skladbot_worker.py`.
- Пересобран и перезапущен только `skladbot-worker`.
- Не трогал:
  - Postgres;
  - `backend-api`;
  - `telegram-worker`;
  - frontend;
  - `version.json`;
  - Windows archive;
  - GitHub Release/push-обновления.
- Проверка:
  - SHA256 файла на VDS совпал с локальным: `63445d4a84fcb92126e7a14448002b628c1d809541bab2d1c669d5cad46ae78c`;
  - VDS `acceptance_status.sh` вернул `status=ok`;
  - `skladbot-worker` running;
  - лог worker: `no active backend orders, skip SkladBot API`;
  - acceptance marker пустой;
  - VDS `version.json` остался `1.1.7`, без download URL.

## Desktop MVP 2.0 Version Marker

- Добавил нижний маркер версии в desktop: `Версия: 1.1.17 · MVP 2.0`.
- Причина: сегодня уже была ситуация, когда локально запустился старый `1.1.7`; теперь свежая ветка визуально отличается.
- `version.json` не менялся, push-обновления не включались.
- Startup self-check пишет `build_label=MVP 2.0`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_startup_check tests.test_desktop_ui_contract` - 7 тестов OK.
  - `.venv/bin/python -m unittest discover -s tests` - 143 теста OK.
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`.
  - `git diff --check` - OK.

## Windows Acceptance Build Label Guard

- Усилил защиту Windows-приёмки от запуска старой сборки.
- Тестовый архив теперь кладёт в `build_manifest.json` не только `app_version`, но и `app_build_label = MVP 2.0`.
- `windows_backend_acceptance.ps1` теперь проверяет:
  - source-запуск: `APP_VERSION >= 1.1.17` и `APP_BUILD_LABEL = MVP 2.0`;
  - exe-запуск: наличие свежего `build_manifest.json`, `app_version >= 1.1.17`, `app_build_label = MVP 2.0`.
- Если запустить старый рабочий `1.1.7` ярлык или старый архив без manifest/build label, helper остановит запуск до открытия приложения.
- Это нужно, чтобы на приёмке не повторилась ситуация “локально открылся 1.1.7, интерфейс старый”.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_windows_test_build_helper tests.test_windows_acceptance_helper tests.test_release_preflight tests.test_startup_check tests.test_desktop_ui_contract` - 18 тестов OK.
  - `version.json` не изменён.

## Desktop Party Summary UI

- Добавил в рабочий экран склада общую статистику выбранной партии.
- Теперь после выбора заказа видно:
  - сколько позиций в партии;
  - общий план в блоках;
  - общую сумму;
  - дату отгрузки;
  - номер заявки SkladBot или `без номера SkladBot`.
- При сбросе выбора показывается `Партия не выбрана`.
- Исправил читаемость кнопки `СЛЕДУЮЩАЯ ПОЗИЦИЯ`: жёлтый фон оставлен, текст сделан чёрным под палитру TakSklad.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_desktop_ui_contract tests.test_startup_check` - 8 тестов OK;
  - `.venv/bin/python -m py_compile src/taksklad/main.py` - OK.

## Telegram Date Display Polish

- Сделал даты в Telegram-кнопках и сообщениях удобными для менеджера.
- Backend продолжает работать с ISO `YYYY-MM-DD`, но в Telegram теперь видно `DD.MM.YYYY`.
- Изменено:
  - кнопки логистики показывают `Логистика 29.05.2026`;
  - `КИЗ по файлам` показывает даты файлов как `29.05.2026`, а не `2026-05-29`.
- API и база не менялись.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_telegram_import` - 18 тестов OK;
  - `.venv/bin/python -m py_compile backend/app/telegram_worker.py` - OK.

## VDS Telegram Worker Date Display Sync

- Доставил Telegram-правку на VDS.
- Синхронизирован только `backend/app/telegram_worker.py`.
- Пересобран и перезапущен `telegram-worker`.
- Docker Compose также пересоздал `backend-api` как зависимость сборки, но Postgres volume и данные не трогались.
- Не трогал:
  - Windows archive;
  - `version.json`;
  - GitHub Release;
  - push-уведомления.
- Проверка на VDS:
  - SHA256 файла совпал с локальным: `16835844a4e37c7e59b39aefa07e721bc9846ab6bf3d571d6386ccbd5964b756`;
  - `https://api.taksklad.uz/health` вернул `status=ok`;
  - `deploy/vds/acceptance_status.sh` вернул `status=ok`;
  - `telegram-worker` running;
  - VDS `version.json` остался `1.1.7`, без download URL.

## SkladBot Window Documentation Clarified

- Проверил runtime-настройки `skladbot-worker` на VDS:
  - `SKLADBOT_CUSTOMER_ID=6211`;
  - `SKLADBOT_SHIPMENT_TYPE_ID=3389`;
  - `SKLADBOT_SYNC_LOOKBACK_DAYS=1`;
  - `SKLADBOT_REQUESTS_LIMIT=100`;
  - `SKLADBOT_WORKER_INTERVAL_SECONDS=60`;
  - `SKLADBOT_API_TIMEOUT_SECONDS=8`;
  - `SKLADBOT_API_MAX_RETRIES=2`.
- Исправил docs: 14-дневное окно относится только к desktop fallback, а серверный worker MVP 2.0 работает узко по вчера/сегодня.
- Это закрывает путаницу между старой desktop-логикой и новой VDS-логикой.

## Shift KIZ Report Part Guard

- Усилил закрытие смены по КИЗам.
- Отчёты по-прежнему делятся по датам отгрузки и получают имена `scan_report_ДД_ММ_ГГГГ_чN.xlsx`.
- Добавлен локальный реестр `reports/shift_report_registry.json`:
  - хранит дату смены, дату отгрузки, номер части, имя файла и хэш содержимого;
  - если склад повторно нажал `Закрыть смену` без новых КИЗов, приложение не создаёт ложный `ч2`;
  - если на следующий день появились новые КИЗы по той же дате отгрузки, формируется следующий файл `ч2`, `ч3` и далее.
- В окне результата повторно найденный файл помечается как `уже был сформирован`.
- `version.json` не менялся, автообновление не включалось.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_daily_report` - OK;
  - `.venv/bin/python -m unittest discover -s tests` - 147 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff --check` - OK.

## Desktop UI Contract Guard

- Зафиксировал тестом правило интерфейса склада:
  - на главном рабочем экране нет кнопки печати;
  - печать появляется только через окно после `ЗАВЕРШИТЬ ЗАКАЗ`;
  - старые кнопки `Импорт Excel`, `Товары`, `Контроль`, `Завершить день` не должны вернуться.
- Это нужно из-за риска открыть старую `1.1.7` или случайно вернуть старый складской экран.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_desktop_ui_contract tests.test_startup_check` - 9 тестов OK.

## User Guide Cleanup For MVP 2.0

- Привёл пользовательскую инструкцию к текущей логике 2.0.
- Убрал из основного процесса старую формулировку `дневной отчёт`.
- Зафиксировал понятную формулировку:
  - закрытие смены формирует КИЗ-отчёты по датам отгрузки;
  - части одной даты получают `ч1`, `ч2` и далее;
  - логистический отчёт выгружается менеджером отдельно и не зависит от склада;
  - печать сводного листа появляется только после `ЗАВЕРШИТЬ ЗАКАЗ`.
- Убрал из инструкции черновые комментарии требований и старые кнопки `Импорт Excel`, `Товары`, `Контроль` из описания основного складского экрана.

## Acceptance Results Current NO-GO

- Создал текущий файл результатов приёмки: `outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md`.
- Статус зафиксирован честно как `NO-GO`, потому что ещё не пройдены:
  - живой Telegram upload от пользовательского аккаунта;
  - живая заявка SkladBot `3PL отгрузка` по acceptance-заказу;
  - физическая Windows-приёмка со сканером и печатью.
- Preflight в этом файле отмечен как пройденный, `version.json` остаётся на `1.1.7`.
- Проверка:
  - `.venv/bin/python tools/release_go_no_go.py --results outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md` ожидаемо вернул `no_go`;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff --check` - OK.
- Синхронизировал `ACCEPTANCE_RESULTS.md` на VDS в `/opt/taksklad/app/outputs/taksklad_acceptance/`.
- VDS `acceptance_status.sh` после синхронизации вернул `status=ok`; контейнеры running, marker пустой, `version.json=1.1.7`.

## VDS Acceptance Release Gate

- Усилил `deploy/vds/acceptance_status.sh`:
  - обычный запуск показывает отдельный блок `release_go_no_go`;
  - `--require-go` включает строгий релизный gate и падает, пока `ACCEPTANCE_RESULTS.md` не заполнен как `GO`;
  - относительный `TAKSKLAD_ENV_FILE` приводится к абсолютному пути;
  - пустой список аргументов verifier больше не ломает bash с `set -u`.
- Синхронизировал на VDS:
  - `deploy/vds/acceptance_status.sh`;
  - `tools/release_go_no_go.py`;
  - `outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md`.
- Проверка:
  - `bash -n deploy/vds/acceptance_status.sh` - OK;
  - `.venv/bin/python -m unittest tests.test_vds_acceptance_scripts tests.test_release_go_no_go tests.test_release_preflight` - 17 тестов OK;
  - `.venv/bin/python -m unittest discover -s tests` - 147 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff --check` - OK;
  - VDS `./deploy/vds/acceptance_status.sh` - `status=ok`, `release_go_no_go.status=no_go`;
  - VDS `./deploy/vds/acceptance_status.sh --require-go` - ожидаемо падает с `release GO/NO-GO is not go: no_go`.

## Acceptance Kit Result File Guard

- Усилил acceptance kit для Windows/VDS-приёмки:
  - `tools/prepare_acceptance_kit.py` теперь создаёт стартовый `ACCEPTANCE_RESULTS.md` как `NO-GO`, если файла ещё нет;
  - уже заполненный `ACCEPTANCE_RESULTS.md` не затирается при повторной генерации kit;
  - `tools/build_windows_test_archive.ps1` кладёт в test archive не только шаблон, но и фактический `ACCEPTANCE_RESULTS.md`;
  - `tools/release_preflight.py` проверяет наличие фактического файла результатов.
- Зачем: чтобы релиз 2.0 нельзя было случайно принять по пустому шаблону или собрать Windows test archive без текущего статуса приёмки.
- Проверка:
  - `.venv/bin/python -m py_compile tools/prepare_acceptance_kit.py tools/release_preflight.py tools/release_go_no_go.py` - OK;
  - `.venv/bin/python tools/prepare_acceptance_kit.py` - OK;
  - `.venv/bin/python -m unittest tests.test_acceptance_excel_generator tests.test_windows_test_build_helper tests.test_release_preflight tests.test_vds_acceptance_scripts tests.test_release_go_no_go` - 25 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `.venv/bin/python tools/release_go_no_go.py --results outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md` - ожидаемо `status=no_go`;
  - `git diff -- version.json` - без изменений;
  - `git diff --check` - OK.
- Синхронизировал обновлённый acceptance kit на VDS.
- Проверка VDS после синхронизации:
  - `./deploy/vds/acceptance_status.sh` - `status=ok`, контейнеры running, marker пустой, `version_json=1.1.7`, `release_go_no_go.status=no_go`;
  - `./deploy/vds/acceptance_status.sh --require-go` - ожидаемо падает с `release GO/NO-GO is not go: no_go`.

## SkladBot Fresh Request Guard

- Усилил фильтр свежих заявок SkladBot в backend worker.
- Был риск: если SkladBot отдаст заявку без `created_at` и `updated_at`, worker считал её свежей и мог проверить старую заявку.
- Теперь такие заявки не попадают в автоматический матчинг. Это соответствует правилу MVP 2.0: искать только свежие заявки за окно вчера/сегодня, а не тащить старый список.
- Зачем: лучше оставить номер SkladBot пустым/`not_found`, чем ошибочно привязать чужую или старую заявку к заказу и потом сломать возвраты.
- Синхронизировал изменение на VDS и пересобрал только `skladbot-worker`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_skladbot_worker tests.test_vds_acceptance_scripts` - 14 тестов OK;
  - `.venv/bin/python -m unittest discover -s tests` - 150 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff -- version.json` - без изменений;
  - `git diff --check` - OK;
  - VDS `./deploy/vds/acceptance_status.sh` - `status=ok`, `skladbot-worker` running, `version_json=1.1.7`, `release_go_no_go.status=no_go`.

## VDS Acceptance Result File Visibility

- Уточнил `deploy/vds/acceptance_status.sh`:
  - имя `result_file` теперь берётся из `acceptance_manifest.json`;
  - если фактический файл результата приёмки отсутствует, статус-чек падает с понятной ошибкой;
  - в JSON-выводе VDS status теперь видно `manifest.result_file = ACCEPTANCE_RESULTS.md`.
- Зачем: чтобы при диагностике было видно, какой именно файл читает GO/NO-GO gate, и чтобы серверный gate не работал по скрытому hardcode.
- Проверка:
  - `bash -n deploy/vds/acceptance_status.sh` - OK;
  - `.venv/bin/python -m unittest tests.test_vds_acceptance_scripts` - OK;
  - `.venv/bin/python -m unittest tests.test_backend_skladbot_worker tests.test_vds_acceptance_scripts tests.test_release_preflight` - 22 теста OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - VDS `./deploy/vds/acceptance_status.sh` - `status=ok`, в manifest есть `result_file`;
  - VDS `./deploy/vds/acceptance_status.sh --require-go` - ожидаемо `status=failed`, потому что `ACCEPTANCE_RESULTS.md` остаётся `NO-GO`.

## Backend Duplicate KIZ Conflict Guard

- Закрыл риск для параллельной работы двух ПК с backend.
- Было: desktop queue считала любой backend `409 Code already scanned` успешной синхронизацией. Это могло скрыть настоящий дубль, если другой ПК уже записал этот КИЗ в другую позицию.
- Стало:
  - повтор того же КИЗа в той же позиции идемпотентен и возвращает успешный `ScanRead` без повторного увеличения счётчика;
  - тот же КИЗ в другой позиции возвращает явный конфликт `Code already scanned in another order item`;
  - desktop backend queue не удаляет такой конфликт как успешно синхронизированный.
- Зачем: при Windows-приёмке и дальнейшем запуске двух ПК дубль КИЗа не должен тихо пропасть из очереди.
- Изменены:
  - `backend/app/orders_service.py`;
  - `src/taksklad/backend_events.py`;
  - `tests/test_backend_api_persistence.py`;
  - `tests/test_backend_bridge.py`;
  - `docs/product-mvp-2.0-plan.md`;
  - `docs/goal-completion-audit.md`;
  - `docs/implementation-log.md`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_api_persistence.BackendApiPersistenceTests.test_scan_create_is_idempotent_for_same_item_and_rejects_cross_order_duplicate tests.test_backend_bridge.BackendBridgeTests.test_backend_queue_keeps_ambiguous_duplicate_scan_conflict` - OK;
  - `.venv/bin/python -m py_compile backend/app/orders_service.py src/taksklad/backend_events.py tests/test_backend_api_persistence.py tests/test_backend_bridge.py` - OK;
  - `.venv/bin/python -m unittest tests.test_backend_api_persistence tests.test_backend_bridge tests.test_desktop_ui_contract` - 27 тестов OK;
  - `.venv/bin/python -m unittest discover -s tests` - 150 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff -- version.json` - без изменений;
  - `git diff --check` - OK.
- Доставил на VDS:
  - `backend/app/orders_service.py`;
  - `src/taksklad/backend_events.py`;
  - связанные tests/docs/отчёт.
- Пересобран и перезапущен только `backend-api`; Postgres volume, frontend, `telegram-worker`, `skladbot-worker`, Windows archive, GitHub Release и `version.json` не трогались.
- Проверка VDS:
  - `curl https://api.taksklad.uz/health` - `status=ok`;
  - `./deploy/vds/acceptance_status.sh` - `status=ok`, `backend-api` running, `version_json=1.1.7`, `release_go_no_go.status=no_go`.

## Desktop Backend Status Indicator

- Добавил на складской desktop-экран отдельный backend status в блоке статистики.
- Возможные состояния:
  - `Backend: выключен`;
  - `Backend: не настроен`;
  - `Backend: ожидает проверки`;
  - `Backend: online, список из VDS`;
  - `Backend: online, запись включена`;
  - `Backend: очередь N`;
  - `Backend: ошибка, очередь N`.
- Статус обновляется после загрузки списка из backend, после фоновой отправки backend queue и при ошибке backend queue.
- Зачем: это закрывает пункт Desktop Backend Bridge из плана 2.0 про понятный backend online/offline/sync pending. При Windows-приёмке сразу видно, что тестовая копия реально работает через VDS.
- Обновил:
  - `src/taksklad/app_day_end.py`;
  - `src/taksklad/main.py`;
  - `tests/test_desktop_ui_contract.py`;
  - `docs/windows-backend-acceptance.md`;
  - `docs/manual-acceptance-runbook.md`;
  - `docs/product-mvp-2.0-plan.md`;
  - `tools/prepare_acceptance_kit.py`;
  - `outputs/taksklad_acceptance/README.md`;
  - `outputs/taksklad_acceptance/ACCEPTANCE_RESULTS_TEMPLATE.md`;
  - `outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_desktop_ui_contract tests.test_backend_bridge` - 15 тестов OK;
  - `.venv/bin/python -m py_compile src/taksklad/main.py src/taksklad/app_day_end.py tests/test_desktop_ui_contract.py` - OK;
  - `.venv/bin/python tools/prepare_acceptance_kit.py` - OK;
  - `.venv/bin/python -m unittest tests.test_desktop_ui_contract tests.test_backend_bridge tests.test_release_go_no_go tests.test_acceptance_excel_generator tests.test_windows_test_build_helper` - 29 тестов OK;
  - `.venv/bin/python -m unittest discover -s tests` - 152 теста OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `.venv/bin/python tools/release_go_no_go.py --results outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md` - ожидаемо `status=no_go`;
  - `git diff -- version.json` - без изменений;
  - `git diff --check` - OK.

## Release GO/NO-GO Template Coverage Guard

- Усилил `tools/release_go_no_go.py`.
- Теперь он проверяет не только отмеченные пункты в `ACCEPTANCE_RESULTS.md`, но и наличие всех обязательных чекбоксов из `ACCEPTANCE_RESULTS_TEMPLATE.md`.
- Если строку обязательной проверки удалить из результата, gate не пропустит релиз и покажет `required acceptance checkbox is missing`.
- Зачем: защита от ложного `GO`, когда файл результата приёмки случайно или вручную стал неполным.
- Изменены:
  - `tools/release_go_no_go.py`;
  - `tests/test_release_go_no_go.py`;
  - `docs/implementation-log.md`;
  - `отчеты/2026-05-31.md`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_release_go_no_go tests.test_vds_acceptance_scripts tests.test_acceptance_excel_generator` - 16 тестов OK;
  - `.venv/bin/python -m unittest discover -s tests` - 153 теста OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `.venv/bin/python tools/release_go_no_go.py --results outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md` - ожидаемо `status=no_go`, потому что ручная Telegram/SkladBot/Windows-приёмка ещё не закрыта;
  - `git diff -- version.json` - без изменений;
  - `git diff --check` - OK.
- Доставил на VDS:
  - `tools/release_go_no_go.py`;
  - `tests/test_release_go_no_go.py`;
  - связанные docs/отчёт;
  - `ACCEPTANCE_RESULTS.md`;
  - `ACCEPTANCE_RESULTS_TEMPLATE.md`.
- Проверка VDS:
  - `python3 -m unittest tests.test_release_go_no_go` - 8 тестов OK;
  - `./deploy/vds/acceptance_status.sh` - `status=ok`;
  - `./deploy/vds/acceptance_status.sh --require-go` - ожидаемо exit `3`, потому что ручная приёмка ещё `NO-GO`;
  - контейнеры running;
  - `version_json=1.1.7`, download URL не задан.

## Telegram Logistics Report Error Message

- Улучшил поведение Telegram-кнопки `Отчёт логистики`.
- Был риск: если backend возвращал 409, например `Missing coordinates`, менеджер видел общий текст ошибки действия, а полезная причина уходила в логи.
- Теперь worker ловит backend HTTP-ошибку и отправляет менеджеру понятное сообщение с датой и причиной backend.
- Пример смысла сообщения: отчёт за дату не выгружен, потому что не хватает координат по конкретному клиенту.
- Изменены:
  - `backend/app/telegram_worker.py`;
  - `tests/test_backend_telegram_import.py`;
  - `docs/implementation-log.md`;
  - `отчеты/2026-05-31.md`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_telegram_import tests.test_backend_api_persistence` - 33 теста OK;
  - `.venv/bin/python -m py_compile backend/app/telegram_worker.py tests/test_backend_telegram_import.py` - OK;
  - `.venv/bin/python -m unittest discover -s tests` - 154 теста OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `.venv/bin/python tools/release_go_no_go.py --results outputs/taksklad_acceptance/ACCEPTANCE_RESULTS.md` - ожидаемо `status=no_go`;
  - `git diff -- version.json` - без изменений;
  - `git diff --check` - OK.
- Доставил на VDS:
  - `backend/app/telegram_worker.py`;
  - `tests/test_backend_telegram_import.py`;
  - docs/отчёт.
- Пересобрал `telegram-worker`; Docker Compose также пересоздал `backend-api` как зависимость образа.
- Не трогал:
  - Postgres volume;
  - frontend;
  - `skladbot-worker`;
  - `version.json`;
  - Windows archive;
  - GitHub Release/push-обновления.
- Проверка VDS:
  - `docker compose exec telegram-worker python -m py_compile /app/app/telegram_worker.py` - OK;
  - `https://api.taksklad.uz/health` - `status=ok`;
  - `./deploy/vds/acceptance_status.sh` - `status=ok`;
  - все контейнеры running;
  - `version_json=1.1.7`, download URL не задан.
- Прямой `python3 -m unittest tests.test_backend_telegram_import` на VDS-хосте не подходит: системный Python сервера без `openpyxl`. Runtime-код проверен внутри контейнера.

## SkladBot Request Type And Address Diagnostic Guard

- Ужесточил фильтр типа заявки SkladBot.
- Теперь заявка не пройдёт matching, если в типе есть возвратные слова: `возврат`, `return`, `returned`.
- Рабочие варианты `3PL отгрузка` и `Отгрузка 3PL` продолжают приниматься.
- Добавил в read-only диагностику поле `address_soft_match`.
- Адрес остаётся мягким признаком: виден в диагностике, но не блокирует совпадение.
- Зачем: снизить риск привязки номера возвратной/нецелевой заявки к заказу на отгрузку.
- Изменены:
  - `backend/app/skladbot_worker.py`;
  - `backend/app/skladbot_diagnostic.py`;
  - `src/taksklad/skladbot.py`;
  - `tests/test_backend_skladbot_worker.py`;
  - `tests/test_skladbot_sync.py`;
  - `docs/implementation-log.md`;
  - `отчеты/2026-05-31.md`.
- Проверка:
  - `.venv/bin/python -m unittest tests.test_backend_skladbot_worker tests.test_skladbot_sync` - 33 теста OK;
  - `.venv/bin/python -m py_compile backend/app/skladbot_worker.py backend/app/skladbot_diagnostic.py src/taksklad/skladbot.py tests/test_backend_skladbot_worker.py tests/test_skladbot_sync.py` - OK;
  - `.venv/bin/python -m unittest discover -s tests` - 156 тестов OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff -- version.json` - без изменений;
  - `git diff --check` - OK.

## TakSklad 2.0.0 Release Version Alignment

- Поднял версии:
  - desktop `APP_VERSION = 2.0.0`;
  - backend `/health` version = `2.0.0`;
  - frontend package = `2.0.0`;
  - Windows acceptance helper теперь ждёт минимум `2.0.0`.
- На VDS восстановил env после неудачной синхронизации:
  - backend host снова `api.taksklad.uz`;
  - frontend временно работает на `https://api.taksklad.uz/`, потому что DNS `app.taksklad.uz` пока NXDOMAIN;
  - API остался на `/api/...` и `/health`;
  - placeholder-секреты заменены на новые значения вне git;
  - пароль Postgres приведён к новому `DATABASE_URL`;
  - локальная копия новых VDS-секретов лежит в ignored-файле `.env.taksklad-vds-2.0.generated.json`.
- Публичный `version.json` пока не переключал: его нужно заполнить после GitHub Release, когда будут готовы реальные download URL и SHA256.
- Проверка:
  - `.venv/bin/python -m unittest discover -s tests` - 156 тестов OK;
  - `npm run build` в `frontend/` - OK;
  - `.venv/bin/python tools/release_preflight.py` - `status=ok`;
  - `git diff --check` - OK.
- Проверка VDS:
  - `https://api.taksklad.uz/health` - 200, version `2.0.0`;
  - internal orders endpoint - 200;
  - frontend без BasicAuth - 401;
  - frontend с BasicAuth - HTML;
  - `./deploy/vds/acceptance_status.sh` - `status=ok`, GO/NO-GO ожидаемо `no_go`.

# TakSklad: текущий статус

Дата сверки: 2026-08-10. Сверка живая: endpoints и GitHub Actions читались
read-only в этот день. Production PostgreSQL в этот день **не читался**, поэтому
раздел «Данные склада» остаётся снимком 2026-08-01

Статус: `LIVE_IN_SYNC_WITH_MAIN; DESKTOP_RETIREMENT_BLOCKED;
OPERATOR_PHYSICAL_NOT_RUN; SHIPMENT_NOT_RECHECKED`

## Короткий вывод

Production работает на том же коммите, что и `main`, релиз
`server-fd05e1a7…` выкачен 2026-08-10

Выпущен единый порядок блокировок для writer-ов заказа и КИЗа
(`backend/app/order_locking.py`). Закрыты пять сценариев параллельной работы
web-панели и десктопа, три из которых давали настоящий `DeadlockDetected`
Postgres, а два теряли работу оператора беззвучно: устаревший скан после `reset`
дозавершал позицию без ошибки, а удаление заказа оставляло осиротевшее движение,
после которого КИЗ блокировался навсегда

Инверсия порядка блокировок из плана целостности КИЗов (пункт 6.18) закрыта: два
теста `tests/test_postgres_kiz_lock_order.py`, стоявшие `expectedFailure`, теперь
проходят по-настоящему

Офлайн-очередь сканов web-панели подключена к операторскому экрану (Task 6)
Поведение очереди при `incomplete` намеренно не менялось, это отдельный вопрос

К отказу от Windows-приложения контур по-прежнему не готов: подтверждённые
пробелы паритета не пересматривались с 2026-07-27.

## Идентичность контуров

Эти идентификаторы нельзя смешивать. Все значения проверены 2026-08-10

| Контур | Идентичность | Источник |
|---|---|---|
| Последний выпущенный коммит | `fd05e1a7a5870e5fbc93cdb6bf58639132a33038` | `git log`, `Deploy Server Production` run `31336015144` |
| Live backend | `2.0.51`, commit `fd05e1a7…`, image `sha256:4ddde3f1…` | `GET /version` |
| `server_release_id` | `server-fd05e1a7…` | `GET /version` |
| Backend в коде | `APP_VERSION = 2.0.51` | `backend/app/settings.py:19` |
| Desktop в коде | `APP_VERSION = 2.0.54` | `src/taksklad/config.py:128` |
| Канал обновления десктопа | `2.0.54`, `min_supported_version 2.0.54`, `mandatory=true` | `raw.githubusercontent.com/1fear/TakSklad/main/version.json` |
| `desktop_api_contract` | `1` | `GET /version` |

Live backend работает на головe `main`: `commit_sha` из `/version` совпадает с
`origin/main`, а `image_digest` совпадает с digest из лога сборки релиза
`31335868550`. Ветка, собранный образ и живой runtime это один релиз

Версия `2.0.51` этим релизом не менялась, идентичность несут `commit_sha` и
`server_release_id`, а не номер версии

Сравнивать `git rev-parse HEAD` с `GET /version` напрямую нельзя: коммиты, не
меняющие runtime (документация, заметки), уходят в `main` без релиза и легально
сдвигают ветку вперёд. Признак реального расхождения — коммит, меняющий
`backend/`, `frontend/` или `deploy/`, которого нет в развёрнутом релизе.

Канал обновления десктопа берётся десктопом с GitHub raw
(`src/taksklad/config.py:132`), а не с VDS. Копия `version.json` в
`/opt/stacks/taksklad/app` устарела до `2.0.25` и используется только
`deploy/vds/acceptance_status.sh`. Из-за этого evidence деплоя показывает
`version_json.latest_version=2.0.25`, что не отражает реальный канал. Копию нужно
привести в соответствие или перестать печатать её в evidence.

## Что изменилось с 2026-08-01

Поверх выпущенного `8376221` в `main` доведено 22 коммита, из них выпущен
`fd05e1a`:

| Коммит | Содержание |
|---|---|
| `fd05e1a` | единый порядок блокировок, пять гонок заказа и КИЗа закрыты (#117) |
| `fb936e5` | офлайн-очередь сканов подключена к операторскому экрану, Task 6 (#116) |
| `431a62e` | `nanoid` до `3.3.18`, снят блокирующий npm-advisory (#115) |
| `46cb9bd` | справочник области матчится только по точному имени (#114) |
| `7604b74` | логистика, отбой после разблокировки цикла (#113) |
| `32aca6a`, `1a2c1a8` | Smartup, транзакции БД убраны из-под сетевых вызовов, токен из логов (#112, #111) |
| `b90883c` | durable офлайн-очередь сканов, фаза 1 (#110) |
| `ab8e247`, `3fe718a`, `cde0779` | четыре дефекта КИЗ, валидация формата марки, освобождение занятого кода (#109, #99) |
| `5583481` | анонимный bootstrap больше не даёт складских прав до подтверждения |
| `d08f6f1`, `84bb3b9` | граница города и разделение отчёта логистики на город и область |
| `0814946`, `7de94c2`, `d51ba36` | forward-миграции в серверном релизе, preflight, свежее DR-учение |

Выпуск `fd05e1a` прошёл штатным циклом: PR #117, зелёный CI с `Release gate`,
immutable release `31335868550`, `Deploy Server Production` `31336015144` с
ручным подтверждением окружения `production`

Ранее, в срезе 2026-08-01, в `main` было доведено 10 коммитов поверх `c3d535bb`:

| Коммит | Содержание |
|---|---|
| `607bcbe` | операторская web-панель склада и параллельная работа с десктопом |
| `6445e32`, `3bc3143`, `fd1c8c0` | план целостности жизненного цикла КИЗов, спецификация guard-ов, read-only прогон по production |
| `7b04402` | единый resolver политики оплаты в shadow-режиме |
| `cd8308c` | машинные коды ошибок скана в десктопе |
| `671b3b6` | разделение заказов Smartup по `deal_id` |
| `4751ffb` | склейка позиций заказа по «ID заказа» и товару |
| `f1536c1` | прогрев endpoint до окна SLO в live-evidence |
| `8376221` | rollback-preflight возвращает окружение кандидата целиком |

Три последних выпущены 2026-07-31 отдельными release-циклами через PR, CI,
immutable release и `Deploy Server Production`.

## Живая проверка 2026-08-10

Снято `2026-08-10T08:14:23Z`, после деплоя

| Проверка | Результат |
|---|---|
| `/health` | `200`, `status=ok` |
| `/version` | `200`, commit `fd05e1a7…`, image `sha256:4ddde3f1…` |
| `/ready` | `200`, `ready=true` |
| База | `status=ok` |
| Воркеры | `3/3`, missing `0`, unhealthy `0` |
| Hot-path очередь | блокеров `0`, зависших `0`, ошибок `0` |
| Миграции | `20260804_0021`, совпадает с ожидаемым head |

Миграция этим релизом не двигалась, политика `no_change`, все три воркера
поднялись после квиесценции

Граница проверки: импорты и доставка дневного отчёта в этот день **не
проверялись**, для них нужен доступ к production PostgreSQL, который не
открывался

## Живая проверка 2026-08-01

| Проверка | Результат |
|---|---|
| `/health` | `200`, `status=ok` |
| `/ready` | `200`, `ready=true`, mandatory policy `ok` |
| Воркеры | `3/3`, missing `0`, unhealthy `0` |
| Hot-path очередь | блокеров `0`, зависших `0`, ошибок `0` |
| Импорты | недавних ошибок `0` |
| Дневной отчёт | `ok`, за 2026-07-31 доставлен |
| Миграции | `20260719_0020`, совпадает с ожидаемым head |

Прошлый блокер `workers=unhealthy` (2026-07-20) не воспроизводится.

## Данные склада, снимок 2026-08-01, не пересверялся

Отгрузка 2026-08-03 закрыта полностью: 81 заказ, все `completed`, 211 позиций,
348 блоков из 348 отсканировано. Незакрытых заказов на будущие даты нет.

Задвоенных позиций (один товар несколькими строками одного заказа) в базе `0`.

## Дефект задвоения позиций и его закрытие

Дедуп позиций смотрел только на «ID импорта», а он считается из sha файла, листа
и номера строки, то есть уникален для каждой строки. Один товар одного заказа мог
занять две позиции. Количество не терялось, но склад видел две одинаковые строки,
а SkladBot одну суммарную.

Дополнительно поле «ID заказа» из Excel не было идентификатором заказа: в хеш
входили `product` и `row`. На боевом импорте 70 строк давали 70 разных
«ID заказа» при 24 реальных заказах.

Решение и разбор: [склейка позиций заказа](order-position-merge-2026-07-31.md).

Починка данных 2026-07-31: 7 групп в 5 заказах, 22 блока до и после, удалено 7
поглощённых позиций, перенесено 2 скан-кода и 2 движения КИЗ, осиротевших
записей `0`. Backup снят до записи.

Ограничение доказательства: после выката прошло 39 импортов, все `smartup_auto`,
`merged_position_rows=0` во всех. Поле присутствует, то есть новый код исполняется,
но сама склейка на боевых данных ещё не срабатывала. Исходный случай пришёл
Telegram-файлом, таких импортов с момента выката не было.

## Release-контур

Две правки закрыли дефекты самого выката.

**Окно SLO мерило холодный старт.** Окно стартовало сразу после пересоздания
backend-контейнера, поэтому первые ответы отражали холодный старт. Два прогона
дали p95 `2027 мс` и `502.6 мс` при бюджете `500 мс`, хотя p50 был `172` и
`164 мс` и ошибок не было. `collect_live` прогревает три endpoint тремя циклами
до окна и эти пробы в измерение не берёт, прогрев пишется в evidence как
`slo.warmup`. Последний деплой: p50 `136 мс`, p95 `438 мс`, `pass`.

**Rollback-preflight терял окружение кандидата.**
`verify_previous_runtime_preflight` подменяет `RELEASE_*` на прошлый релиз, чтобы
доказать возможность отката, а возвращала назад только 4 переменные из 6, которые
переносит `export_release_runtime_env`. От прошлого релиза оставались
`RELEASE_SERVER_RELEASE_ID` и `RELEASE_DESKTOP_API_CONTRACT`. Backend уезжал в
production с release id прошлого релиза, из-за чего compose пересоздавал
`backend-api` на каждом деплое даже при идентичном образе. В отчётности дефект не
был виден: `health_service` подставляет `server-{commit_sha}`, когда настроенный
id не совпадает с коммитом. Окружение кандидата теперь восстанавливается целиком.

Проверено после выката: config-hash работающего контейнера и ожидаемого совпадают,
`docker compose up --dry-run` для того же релиза даёт `Running`, а не `Recreate`.

## Квиесценция писателей при деплое

Деплой останавливает `backend-api` и три воркера на время cutover-backup и
миграции. Измеренное окно записи 2026-07-31: `15.5 с`, из них бэкап `8.9 с`,
миграция `2.6 с`, восстановление лизов `1.6 с`.

Оценка выноса квиесценции при `no_change` выполнена 2026-08-01, вывод
отрицательный:

- `MIGRATION_POLICY = "no_change"` это константа `tools/server_release_artifacts.py:26`,
  проверяемая при валидации манифеста и в workflow. Другого значения у серверного
  релиза не бывает, поэтому условие всегда истинно и означает снятие защиты целиком;
- `recover_inflight_event_leases_after_worker_stop` (`backend/app/event_leases.py:261`)
  перезапускает все события со `status='processing'` и непустым `lease_owner`
  **без проверки `lease_expires_at`**. С живыми воркерами это отберёт лизы у
  событий, которые обрабатываются, и даст дубли `skladbot_request_create` и
  `telegram_notification`.

Единственный вариант с приемлемым соотношением, если окно всё же важно: вынести
`pg_dump -Fc` до остановки писателей. Дамп берёт консистентный MVCC-снимок и в
квиесценции для консистентности не нуждается, это экономит `8.9 с` из `15.5 с`.
Цена решения: точка отката перестаёт быть точкой без незавершённой работы.
Отдельного решения владельца нет, изменение не выполнялось.

## Источники истины

| Слой | Статус | Evidence и граница |
|---|---|---|
| Docs truth | `PASS` | Этот документ сверен с кодом, git и live 2026-08-10. |
| Code truth | `PASS` | `main`, `origin/main` и live backend на одном SHA `fd05e1a7…`. |
| Test truth | `PASS` | Python `1812 passed, 92 skipped, 4 xfailed`; PostgreSQL `109 passed`, ноль `expectedFailure`, в одноразовом PostgreSQL 16.14 (2026-08-10). |
| Live truth | `PASS` | `/health`, `/ready`, `/version` прочитаны 2026-08-10 после деплоя. |
| Data truth | `STALE` | Production PostgreSQL 2026-08-10 не читался. Последнее чтение 2026-08-01, последняя запись 2026-08-06 по явному разрешению. |
| Release truth | `PASS` | `Deploy Server Production` `31336015144` завершился успешно, все 14 шагов зелёные. |
| Parity truth | `FAIL_FOR_RETIREMENT` | 121 подтверждённый пробел, 15 из них P1. Не пересматривалось с 2026-07-27, см. [паритет](web-desktop-parity.md). |
| Operator truth | `NOT RUN` | Рабочее место и реальный складской процесс не проверялись. |
| Physical truth | `NOT RUN` | Handheld scanner, системный print dialog и бумажная печать не проверялись. |

## Открытые пункты

1. **Копия `version.json` на VDS устарела** до `2.0.25` и попадает в evidence
   деплоя. Реальный канал десктопа отдаёт `2.0.54`. Привести копию в соответствие
   или убрать её из evidence.
2. **5 событий `skladbot_daily_report_send` в статусе `failed`**, с 2026-07-06 по
   2026-07-21. Новых после 21.07 нет, hot-path не блокируют, разбор не проводился.
3. **18 открытых инцидентов**, все июльские. Августовских нет.
4. **Склейка позиций не подтверждена боевыми данными**: нужен Telegram-импорт с
   повтором товара внутри одного заказа, чтобы `merged_position_rows` стал
   ненулевым.
5. **Отказ от Windows-приложения остаётся заблокированным** пробелами паритета.
6. **Операторский и физический контур** (сканер, печать, рабочее место) не
   проверялись ни в одной из последних сверок.
7. **Поведение офлайн-очереди десктопа при `incomplete` не решено.** Событие
   `order_complete`, отклонённое как `incomplete`, уходит в durable-секцию
   `blocked_backend_events` и в основную очередь не возвращается. Альтернатива с
   удержанием события в очереди подготовлена, но не выпущена: она меняет то, что
   видит оператор, и требует отдельного согласования
8. **Пункты 2, 3 и 4 не пересверялись 2026-08-10**, они остаются снимком
   2026-08-01, потому что доступ к production PostgreSQL в этот день не
   открывался

## Читать дальше

1. [Склейка позиций заказа](order-position-merge-2026-07-31.md).
2. [Разделение заказов Smartup по deal_id](smartup-deal-split-design-2026-07-30.md).
3. [Паритет web и desktop](web-desktop-parity.md).
4. [Приёмка веб-склада](web-warehouse-acceptance.md).
5. [Deploy и rollback](deploy-rollback-runbook.md).
6. [Server topology и live endpoints](SERVER.md).
7. [Доступы без секретов](ACCESS.md).
